
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-simple-spring-boot-app using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=existsById_28aaa34d3a
ROOST_METHOD_SIG_HASH=existsById_83b9bf1592

Scenario 1: User Exists With Valid ID

Details:
  TestName: existsByIdReturnsTrueWhenUserExists
  Description: This test verifies that the existsById method returns true when a user with the specified ID exists in the repository. The test checks the basic positive scenario where the users map contains an entry with the given key.
Execution:
  Arrange: Create a UserRepository instance which initializes with sample data (Alice, Bob, and Charlie). Identify a valid user ID that exists in the repository (e.g., ID 1, 2, or 3 from the constructor initialization).
  Act: Invoke the existsById method with a valid user ID that exists in the users map.
  Assert: Use assertTrue to verify that the method returns true, confirming the user exists.
Validation:
  The assertion verifies that the containsKey method on the ConcurrentHashMap correctly identifies existing entries. This is critical for ensuring that the repository can accurately determine user existence before performing operations like updates or retrievals. This test validates the fundamental functionality of the existence check mechanism.

Scenario 2: User Does Not Exist With Invalid ID

Details:
  TestName: existsByIdReturnsFalseWhenUserDoesNotExist
  Description: This test ensures that the existsById method returns false when queried with an ID that does not exist in the repository. This validates the negative case where the users map does not contain the specified key.
Execution:
  Arrange: Create a UserRepository instance with its default sample data. Choose an ID that is guaranteed not to exist in the repository (e.g., 999 or any large number beyond the initialized users).
  Act: Call the existsById method with a non-existent user ID.
  Assert: Use assertFalse to verify that the method returns false, confirming the user does not exist.
Validation:
  This assertion confirms that the containsKey method properly returns false for missing entries. This is essential for preventing false positives in existence checks, which could lead to incorrect application behavior such as attempting to update non-existent records or skipping necessary validations.

Scenario 3: Check Existence With Null ID

Details:
  TestName: existsByIdWithNullIdHandling
  Description: This test examines the behavior of existsById when passed a null ID value. Since the method uses containsKey on a ConcurrentHashMap, this test verifies how null keys are handled.
Execution:
  Arrange: Create a UserRepository instance with default initialization.
  Act: Invoke the existsById method with a null value as the ID parameter.
  Assert: Use assertFalse to verify that the method returns false, as ConcurrentHashMap does not allow null keys and containsKey will return false for null.
Validation:
  This test validates the method's behavior with invalid input. ConcurrentHashMap throws NullPointerException when attempting to use null keys in most operations, but containsKey specifically returns false for null. This test ensures the method handles edge cases gracefully and prevents potential NullPointerExceptions in the application flow.

Scenario 4: Check Existence After User Creation

Details:
  TestName: existsByIdReturnsTrueAfterUserSaved
  Description: This test verifies that after saving a new user to the repository, the existsById method correctly identifies the newly created user. This validates the integration between the save and existsById methods.
Execution:
  Arrange: Create a UserRepository instance. Create a new User object with valid data (name, email, role) but without an ID. Use the save method to persist the user and capture the returned user with the assigned ID.
  Act: Call the existsById method with the ID of the newly saved user.
  Assert: Use assertTrue to confirm that the method returns true for the newly created user.
Validation:
  This assertion ensures that the save operation properly adds entries to the users map and that existsById can immediately detect these new entries. This is crucial for workflows where user creation is followed by existence verification, such as in the saveWithValidation method which internally calls existsById.

Scenario 5: Check Existence After User Deletion

Details:
  TestName: existsByIdReturnsFalseAfterUserDeleted
  Description: This test confirms that after deleting a user from the repository, the existsById method correctly returns false for that user's ID. This validates the consistency between delete and existence check operations.
Execution:
  Arrange: Create a UserRepository instance with default sample data. Identify a valid user ID from the initialized data. Use the deleteById method to remove the user from the repository.
  Act: Invoke the existsById method with the ID of the deleted user.
  Assert: Use assertFalse to verify that the method returns false, confirming the user no longer exists.
Validation:
  This test ensures that the delete operation properly removes entries from the users map and that existsById accurately reflects this change. This is essential for maintaining data consistency and preventing operations on deleted records, which is particularly important in the saveWithValidation method's logic.

Scenario 6: Check Existence With Zero ID

Details:
  TestName: existsByIdWithZeroId
  Description: This test examines the behavior of existsById when passed an ID value of zero. Since the idGenerator starts at 1, zero should not be a valid user ID in normal circumstances.
Execution:
  Arrange: Create a UserRepository instance with its default initialization. Note that the AtomicLong idGenerator starts at 1, so zero should not be assigned to any user.
  Act: Call the existsById method with an ID value of 0L.
  Assert: Use assertFalse to verify that the method returns false, as no user should have ID zero.
Validation:
  This assertion validates that the method correctly handles boundary values. Since the ID generation starts at 1, zero represents an invalid or unassigned ID. This test ensures that the existence check doesn't produce false positives for edge case ID values that fall outside the normal range of generated IDs.

Scenario 7: Check Existence With Negative ID

Details:
  TestName: existsByIdWithNegativeId
  Description: This test verifies the behavior of existsById when provided with a negative ID value. Negative IDs should not exist in the repository as the AtomicLong generator produces only positive values.
Execution:
  Arrange: Create a UserRepository instance with default sample data. The idGenerator uses AtomicLong starting from 1, ensuring all generated IDs are positive.
  Act: Invoke the existsById method with a negative ID value (e.g., -1L or -100L).
  Assert: Use assertFalse to confirm that the method returns false for negative ID values.
Validation:
  This test ensures the method handles invalid ID ranges appropriately. Since the repository's ID generation mechanism only produces positive values, negative IDs represent invalid input. This test confirms that the containsKey method correctly returns false for keys that cannot exist in the normal operation of the repository.

Scenario 8: Check Existence Multiple Times For Same ID

Details:
  TestName: existsByIdConsistentResultsForMultipleCalls
  Description: This test verifies that calling existsById multiple times with the same ID produces consistent results. This validates the idempotent nature of the existence check operation.
Execution:
  Arrange: Create a UserRepository instance with default initialization. Select a valid user ID from the initialized data.
  Act: Call the existsById method multiple times (e.g., three times) with the same valid user ID.
  Assert: Use assertTrue for each invocation to verify that all calls return true consistently.
Validation:
  This assertion confirms that the existsById method is idempotent and thread-safe, producing consistent results across multiple invocations. Since the underlying data structure is ConcurrentHashMap, this test also implicitly validates that the containsKey operation is safe for concurrent access, which is important in multi-threaded Spring applications.

Scenario 9: Check Existence For All Initialized Users

Details:
  TestName: existsByIdForAllInitializedUsers
  Description: This test verifies that existsById returns true for all users initialized in the constructor. This ensures that the initialization process correctly populates the users map.
Execution:
  Arrange: Create a UserRepository instance which initializes with three sample users (Alice, Bob, and Charlie). Use the findAll method to retrieve all initialized users and their IDs.
  Act: Iterate through all retrieved users and call existsById for each user's ID.
  Assert: Use assertTrue for each user ID to verify that all initialized users are detected as existing.
Validation:
  This comprehensive test validates that the constructor's initialization logic properly saves all sample users and that existsById can detect each one. This is important for ensuring data integrity from the moment the repository is instantiated, which is critical for application startup and initial state validation.

Scenario 10: Check Existence With Maximum Long Value

Details:
  TestName: existsByIdWithMaximumLongValue
  Description: This test examines the behavior of existsById when provided with the maximum possible Long value (Long.MAX_VALUE). This tests the method's handling of extreme boundary values.
Execution:
  Arrange: Create a UserRepository instance with default sample data. Note that no user should have an ID equal to Long.MAX_VALUE under normal circumstances.
  Act: Call the existsById method with Long.MAX_VALUE as the ID parameter.
  Assert: Use assertFalse to verify that the method returns false, as this ID should not exist in the repository.
Validation:
  This test validates the method's robustness when handling extreme boundary values. While Long.MAX_VALUE is a valid Long value, it's highly unlikely to be assigned by the AtomicLong generator in normal operation. This test ensures that the containsKey method handles such extreme values without errors or unexpected behavior.

Scenario 11: Check Existence Integration With SaveWithValidation Method

Details:
  TestName: existsByIdUsedInSaveWithValidationMethod
  Description: This test verifies that existsById correctly integrates with the saveWithValidation method, which internally calls existsById to check for existing users before saving.
Execution:
  Arrange: Create a UserRepository instance. Create and save a new User object to get an assigned ID. Create another User object with the same ID but different data.
  Act: Attempt to call saveWithValidation with the duplicate ID user and allowUpdate set to false, which should trigger the internal existsById check.
  Assert: Use assertThrows to verify that an IllegalStateException is thrown, confirming that existsById correctly detected the existing user.
Validation:
  This test validates the integration between existsById and other repository methods that depend on it. The saveWithValidation method relies on existsById to prevent duplicate entries, so this test ensures that the existence check properly supports this business logic. This is crucial for maintaining data integrity and enforcing validation rules.

Scenario 12: Check Existence After Repository Count Changes

Details:
  TestName: existsByIdAccuracyAfterRepositoryModifications
  Description: This test verifies that existsById maintains accuracy as the repository undergoes multiple modifications (additions and deletions), ensuring the method reflects the current state of the users map.
Execution:
  Arrange: Create a UserRepository instance with default data. Save a new user and capture its ID. Delete one of the initialized users. Save another new user.
  Act: Call existsById for the newly added user IDs (should return true), the deleted user ID (should return false), and remaining initialized user IDs (should return true).
  Assert: Use assertTrue for existing users and assertFalse for the deleted user to verify correct existence detection after multiple repository state changes.
Validation:
  This comprehensive test ensures that existsById accurately reflects the current state of the repository regardless of how many modifications have occurred. This is essential for maintaining data consistency in dynamic applications where users are frequently added and removed, and validates that the ConcurrentHashMap's containsKey method properly tracks these changes.

*/

// ********RoostGPT********

package com.example.demo.repository;

import com.example.demo.model.User;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.stereotype.Repository;
import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicLong;

class UserRepositoryExistsByIdTest {

	private UserRepository userRepository;

	@BeforeEach
	void setUp() {
		userRepository = new UserRepository();
	}

	@Test
	@Tag("valid")
	void existsByIdReturnsTrueWhenUserExists() {
		// Arrange
		Long validUserId = 1L;
		// Act
		boolean result = userRepository.existsById(validUserId);
		// Assert
		assertTrue(result);
	}

	@Test
	@Tag("invalid")
	void existsByIdReturnsFalseWhenUserDoesNotExist() {
		// Arrange
		Long nonExistentUserId = 999L;
		// Act
		boolean result = userRepository.existsById(nonExistentUserId);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("boundary")
	void existsByIdWithNullIdHandling() {
		// Arrange
		Long nullId = null;
		// Act
		boolean result = userRepository.existsById(nullId);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("integration")
	void existsByIdReturnsTrueAfterUserSaved() {
		// Arrange
		User newUser = new User("David Wilson", "david@example.com", "USER");
		User savedUser = userRepository.save(newUser);
		// Act
		boolean result = userRepository.existsById(savedUser.getId());
		// Assert
		assertTrue(result);
		assertNotNull(savedUser.getId());
	}

	@Test
	@Tag("integration")
	void existsByIdReturnsFalseAfterUserDeleted() {
		// Arrange
		Long existingUserId = 1L;
		assertTrue(userRepository.existsById(existingUserId));
		userRepository.deleteById(existingUserId);
		// Act
		boolean result = userRepository.existsById(existingUserId);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("boundary")
	void existsByIdWithZeroId() {
		// Arrange
		Long zeroId = 0L;
		// Act
		boolean result = userRepository.existsById(zeroId);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("boundary")
	void existsByIdWithNegativeId() {
		// Arrange
		Long negativeId = -1L;
		// Act
		boolean result = userRepository.existsById(negativeId);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("valid")
	void existsByIdConsistentResultsForMultipleCalls() {
		// Arrange
		Long validUserId = 2L;
		// Act
		boolean firstCall = userRepository.existsById(validUserId);
		boolean secondCall = userRepository.existsById(validUserId);
		boolean thirdCall = userRepository.existsById(validUserId);
		// Assert
		assertTrue(firstCall);
		assertTrue(secondCall);
		assertTrue(thirdCall);
	}

	@Test
	@Tag("valid")
	void existsByIdForAllInitializedUsers() {
		// Arrange
		List<User> allUsers = userRepository.findAll();
		// Act & Assert
		for (User user : allUsers) {
			boolean result = userRepository.existsById(user.getId());
			assertTrue(result);
			assertNotNull(user.getId());
			assertTrue(user.getId() > 0);
		}
	}

	@Test
	@Tag("boundary")
	void existsByIdWithMaximumLongValue() {
		// Arrange
		Long maxLongValue = Long.MAX_VALUE;
		// Act
		boolean result = userRepository.existsById(maxLongValue);
		// Assert
		assertFalse(result);
	}

	@Test
	@Tag("integration")
	void existsByIdUsedInSaveWithValidationMethod() {
		// Arrange
		User newUser = new User("Emma Davis", "emma@example.com", "USER");
		User savedUser = userRepository.save(newUser);
		User duplicateUser = new User("Emma Davis Updated", "emma@example.com", "ADMIN");
		duplicateUser.setId(savedUser.getId());
		// Act & Assert
		IllegalStateException exception = assertThrows(IllegalStateException.class, () -> {
			userRepository.saveWithValidation(duplicateUser, false);
		});
		assertTrue(exception.getMessage().contains("User already exists with id: " + savedUser.getId()));
		assertTrue(userRepository.existsById(savedUser.getId()));
	}

	@Test
	@Tag("integration")
	void existsByIdAccuracyAfterRepositoryModifications() {
		// Arrange
		User newUser1 = new User("Frank Miller", "frank@example.com", "USER");
		User savedUser1 = userRepository.save(newUser1);
		Long newUserId1 = savedUser1.getId();
		Long deletedUserId = 1L;
		userRepository.deleteById(deletedUserId);
		User newUser2 = new User("Grace Lee", "grace@example.com", "ADMIN");
		User savedUser2 = userRepository.save(newUser2);
		Long newUserId2 = savedUser2.getId();
		// Act & Assert
		assertTrue(userRepository.existsById(newUserId1));
		assertTrue(userRepository.existsById(newUserId2));
		assertFalse(userRepository.existsById(deletedUserId));
		assertTrue(userRepository.existsById(2L));
		assertTrue(userRepository.existsById(3L));
	}

}