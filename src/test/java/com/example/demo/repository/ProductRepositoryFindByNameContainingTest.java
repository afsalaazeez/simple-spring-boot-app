
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-simple-spring-boot-app using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=findByNameContaining_255b34584f
ROOST_METHOD_SIG_HASH=findByNameContaining_739e7ba865

Scenario 1: Find Products with Exact Name Match

Details:
  TestName: findProductsWithExactNameMatch
  Description: This test verifies that the method correctly finds products when the search term exactly matches a product name in the repository. It tests the basic functionality of the search method with a complete product name.
Execution:
  Arrange: Set up the ProductRepository with sample data already initialized in the constructor. Use "Laptop" as the search term which exactly matches one of the products.
  Act: Invoke findByNameContaining("Laptop") method.
  Assert: Verify that the returned list contains exactly one product and that product's name is "Laptop".
Validation:
  The assertion verifies that exact name matches are correctly identified by the search method. This is important because users should be able to find products by typing their complete names. The test ensures the case-insensitive comparison works correctly for exact matches.

---

Scenario 2: Find Products with Partial Name Match

Details:
  TestName: findProductsWithPartialNameMatch
  Description: This test checks if the method can find products when only a portion of the product name is provided as the search term. It validates the "containing" functionality of the search.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "top" as the search term which is contained in "Laptop".
  Act: Invoke findByNameContaining("top") method.
  Assert: Verify that the returned list contains at least one product and that the product name contains "top" (case-insensitive).
Validation:
  This assertion confirms that partial string matching works correctly, which is essential for user-friendly search functionality. Users often search with incomplete product names, and this test ensures they can still find relevant products.

---

Scenario 3: Find Products with Case Insensitive Search

Details:
  TestName: findProductsWithCaseInsensitiveSearch
  Description: This test validates that the search method performs case-insensitive matching, allowing users to search regardless of letter casing.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "MOUSE" (all uppercase) as the search term.
  Act: Invoke findByNameContaining("MOUSE") method.
  Assert: Verify that the returned list contains the "Mouse" product, confirming case-insensitive matching.
Validation:
  The assertion ensures that the toLowerCase() conversion works correctly for both the search term and product names. This is critical for user experience as users shouldn't need to worry about exact casing when searching.

---

Scenario 4: Find Products with Mixed Case Search Term

Details:
  TestName: findProductsWithMixedCaseSearchTerm
  Description: This test verifies that the method handles search terms with mixed case letters correctly and still returns matching products.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "KeYbOaRd" (mixed case) as the search term.
  Act: Invoke findByNameContaining("KeYbOaRd") method.
  Assert: Verify that the returned list contains the "Keyboard" product.
Validation:
  This test confirms that the case normalization works correctly regardless of how the user types the search term. It's important for ensuring consistent search results across different input styles.

---

Scenario 5: Find Multiple Products with Common Substring

Details:
  TestName: findMultipleProductsWithCommonSubstring
  Description: This test checks if the method returns all products that contain the search term when multiple products match.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "o" as the search term which appears in multiple product names (Monitor, Mouse, Headphones).
  Act: Invoke findByNameContaining("o") method.
  Assert: Verify that the returned list contains multiple products and all returned products have names containing "o".
Validation:
  This assertion validates that the filter operation correctly identifies all matching products, not just the first one. It's essential for comprehensive search results when multiple products match the criteria.

---

Scenario 6: Find Products with Empty String Search

Details:
  TestName: findProductsWithEmptyStringSearch
  Description: This test verifies the behavior when an empty string is provided as the search term. Since all strings contain an empty string, all products should be returned.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "" (empty string) as the search term.
  Act: Invoke findByNameContaining("") method.
  Assert: Verify that the returned list contains all products in the repository (5 products based on the constructor initialization).
Validation:
  This test ensures that edge case handling for empty strings works correctly. Since every string contains an empty string, returning all products is the expected behavior and is useful for "show all" scenarios.

---

Scenario 7: Find Products with No Matching Results

Details:
  TestName: findProductsWithNoMatchingResults
  Description: This test validates that the method returns an empty list when no products match the search term.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "Smartphone" as the search term which doesn't match any product names.
  Act: Invoke findByNameContaining("Smartphone") method.
  Assert: Verify that the returned list is empty (size equals 0).
Validation:
  This assertion confirms that the method handles non-matching searches gracefully by returning an empty list rather than null or throwing an exception. This is important for preventing null pointer exceptions in calling code.

---

Scenario 8: Find Products with Single Character Search

Details:
  TestName: findProductsWithSingleCharacterSearch
  Description: This test checks if the method correctly handles single character search terms and returns all products containing that character.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "M" as the search term.
  Act: Invoke findByNameContaining("M") method.
  Assert: Verify that the returned list contains products whose names contain "M" or "m" (Mouse, Monitor).
Validation:
  This test ensures that even minimal search input produces correct results. Single character searches are common when users start typing, and the method should handle them efficiently.

---

Scenario 9: Find Products with Special Characters in Search Term

Details:
  TestName: findProductsWithSpecialCharactersInSearchTerm
  Description: This test verifies the behavior when the search term contains special characters that don't exist in any product names.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "@#$" as the search term.
  Act: Invoke findByNameContaining("@#$") method.
  Assert: Verify that the returned list is empty since no product names contain these special characters.
Validation:
  This assertion ensures that the method handles special characters safely without throwing exceptions. It's important for robustness when dealing with user input that may contain unexpected characters.

---

Scenario 10: Find Products with Whitespace in Search Term

Details:
  TestName: findProductsWithWhitespaceInSearchTerm
  Description: This test checks if the method correctly handles search terms containing whitespace characters.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "less mouse" as the search term which matches "Wireless mouse".
  Act: Invoke findByNameContaining("less mouse") method.
  Assert: Verify that the returned list contains the "Mouse" product with description "Wireless mouse".
Validation:
  This test validates that multi-word search terms work correctly. Users often search with phrases, and the method should handle spaces in the search term appropriately.

---

Scenario 11: Find Products with Leading and Trailing Spaces

Details:
  TestName: findProductsWithLeadingAndTrailingSpaces
  Description: This test verifies that the method handles search terms with leading and trailing whitespace correctly.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "  Laptop  " (with leading and trailing spaces) as the search term.
  Act: Invoke findByNameContaining("  Laptop  ") method.
  Assert: Verify that the returned list contains the "Laptop" product, showing that spaces don't prevent matching.
Validation:
  This assertion confirms that the method performs matching based on the actual content, and leading/trailing spaces in the search term don't prevent finding matches. This is important for handling user input that may inadvertently include extra spaces.

---

Scenario 12: Find Products After Adding New Product

Details:
  TestName: findProductsAfterAddingNewProduct
  Description: This test validates that the search method correctly finds newly added products, ensuring the method works with the dynamic state of the repository.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Add a new product with name "Tablet" using the save() method. Use "Tablet" as the search term.
  Act: Invoke findByNameContaining("Tablet") method.
  Assert: Verify that the returned list contains the newly added "Tablet" product.
Validation:
  This test ensures that the search method operates on the current state of the products map and correctly includes newly added products. It's crucial for verifying that the method works correctly as the repository's data changes over time.

---

Scenario 13: Find Products with Numeric Characters in Search

Details:
  TestName: findProductsWithNumericCharactersInSearch
  Description: This test checks if the method can find products when the search term contains numeric characters that appear in product names.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "27" as the search term which appears in "27-inch 4K monitor".
  Act: Invoke findByNameContaining("27") method.
  Assert: Verify that the returned list contains the "Monitor" product.
Validation:
  This assertion validates that the search works with alphanumeric content, not just alphabetic characters. Many product names include numbers (model numbers, sizes, etc.), so this functionality is essential.

---

Scenario 14: Find Products with Hyphenated Search Term

Details:
  TestName: findProductsWithHyphenatedSearchTerm
  Description: This test verifies that the method correctly handles search terms containing hyphens, which are common in product names.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "27-inch" as the search term.
  Act: Invoke findByNameContaining("27-inch") method.
  Assert: Verify that the returned list contains the "Monitor" product whose description includes "27-inch".
Validation:
  This test ensures that special characters like hyphens are handled correctly in the search. Hyphens are commonly used in product specifications and the search should work seamlessly with them.

---

Scenario 15: Find Products in Empty Repository

Details:
  TestName: findProductsInEmptyRepository
  Description: This test validates the behavior when searching in a repository with no products.
Execution:
  Arrange: Create a new ProductRepository instance and delete all pre-initialized products using deleteById() for each product ID from 1 to 5. Use "Laptop" as the search term.
  Act: Invoke findByNameContaining("Laptop") method.
  Assert: Verify that the returned list is empty.
Validation:
  This assertion confirms that the method handles empty repositories gracefully without throwing exceptions. It's important for robustness, especially during application initialization or after bulk deletions.

---

Scenario 16: Find Products with Very Long Search Term

Details:
  TestName: findProductsWithVeryLongSearchTerm
  Description: This test checks the method's behavior when provided with an unusually long search term that doesn't match any products.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use a very long string (e.g., 100+ characters) as the search term.
  Act: Invoke findByNameContaining() method with the long search term.
  Assert: Verify that the returned list is empty since no product names would contain such a long string.
Validation:
  This test ensures that the method handles edge cases with long input strings without performance issues or exceptions. It's important for security and stability when dealing with potentially malicious or erroneous input.

---

Scenario 17: Find Products with Substring at Beginning of Name

Details:
  TestName: findProductsWithSubstringAtBeginningOfName
  Description: This test verifies that the method finds products when the search term matches the beginning of a product name.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "Head" as the search term which matches the beginning of "Headphones".
  Act: Invoke findByNameContaining("Head") method.
  Assert: Verify that the returned list contains the "Headphones" product.
Validation:
  This assertion confirms that the contains() method works correctly for prefixes. Users often start typing product names from the beginning, so this is a common search pattern that must work correctly.

---

Scenario 18: Find Products with Substring at End of Name

Details:
  TestName: findProductsWithSubstringAtEndOfName
  Description: This test validates that the method finds products when the search term matches the end of a product name.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "board" as the search term which matches the end of "Keyboard".
  Act: Invoke findByNameContaining("board") method.
  Assert: Verify that the returned list contains the "Keyboard" product.
Validation:
  This test ensures that the contains() method works correctly for suffixes. This is important because users might remember only the end of a product name or search by product category suffixes.

---

Scenario 19: Find Products with Substring in Middle of Name

Details:
  TestName: findProductsWithSubstringInMiddleOfName
  Description: This test checks if the method finds products when the search term appears in the middle of a product name.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "onit" as the search term which appears in the middle of "Monitor".
  Act: Invoke findByNameContaining("onit") method.
  Assert: Verify that the returned list contains the "Monitor" product.
Validation:
  This assertion validates that the search works for substrings regardless of their position within the product name. This comprehensive matching capability is essential for flexible search functionality.

---

Scenario 20: Find Products with Repeated Search Calls

Details:
  TestName: findProductsWithRepeatedSearchCalls
  Description: This test verifies that multiple consecutive calls to the method with the same search term produce consistent results.
Execution:
  Arrange: Use the pre-initialized ProductRepository. Use "Mouse" as the search term.
  Act: Invoke findByNameContaining("Mouse") method twice consecutively.
  Assert: Verify that both calls return lists with the same size and contain the same product(s).
Validation:
  This test ensures that the method is idempotent and doesn't have side effects that would cause different results on repeated calls. This is important for reliability and predictability of the search functionality.

*/

// ********RoostGPT********

package com.example.demo.repository;

import com.example.demo.model.Product;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import java.math.BigDecimal;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.stereotype.Repository;
import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicLong;

class ProductRepositoryFindByNameContainingTest {

	private ProductRepository productRepository;

	@BeforeEach
	void setUp() {
		productRepository = new ProductRepository();
	}

	@Test
	@Tag("valid")
	void findProductsWithExactNameMatch() {
		List<Product> result = productRepository.findByNameContaining("Laptop");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Laptop", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findProductsWithPartialNameMatch() {
		List<Product> result = productRepository.findByNameContaining("top");

		assertNotNull(result);
		assertTrue(result.size() >= 1);
		assertTrue(result.stream().anyMatch(p -> p.getName().toLowerCase().contains("top")));
	}

	@Test
	@Tag("valid")
	void findProductsWithCaseInsensitiveSearch() {
		List<Product> result = productRepository.findByNameContaining("MOUSE");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Mouse", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findProductsWithMixedCaseSearchTerm() {
		List<Product> result = productRepository.findByNameContaining("KeYbOaRd");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Keyboard", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findMultipleProductsWithCommonSubstring() {
		List<Product> result = productRepository.findByNameContaining("o");

		assertNotNull(result);
		assertTrue(result.size() > 1);
		assertTrue(result.stream().allMatch(p -> p.getName().toLowerCase().contains("o")));
	}

	@Test
	@Tag("boundary")
	void findProductsWithEmptyStringSearch() {
		List<Product> result = productRepository.findByNameContaining("");

		assertNotNull(result);
		assertEquals(5, result.size());
	}

	@Test
	@Tag("valid")
	void findProductsWithNoMatchingResults() {
		List<Product> result = productRepository.findByNameContaining("Smartphone");

		assertNotNull(result);
		assertEquals(0, result.size());
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("boundary")
	void findProductsWithSingleCharacterSearch() {
		List<Product> result = productRepository.findByNameContaining("M");

		assertNotNull(result);
		assertTrue(result.size() >= 2);
		assertTrue(result.stream().allMatch(p -> p.getName().toLowerCase().contains("m")));
	}

	@Test
	@Tag("invalid")
	void findProductsWithSpecialCharactersInSearchTerm() {
		List<Product> result = productRepository.findByNameContaining("@#$");

		assertNotNull(result);
		assertEquals(0, result.size());
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("valid")
	void findProductsWithWhitespaceInSearchTerm() {
		List<Product> result = productRepository.findByNameContaining("less mouse");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Mouse", result.get(0).getName());
		assertTrue(result.get(0).getDescription().contains("Wireless mouse"));
	}

	@Test
	@Tag("boundary")
	void findProductsWithLeadingAndTrailingSpaces() {
		List<Product> result = productRepository.findByNameContaining("  Laptop  ");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Laptop", result.get(0).getName());
	}

	@Test
	@Tag("integration")
	void findProductsAfterAddingNewProduct() {
		Product newProduct = new Product("Tablet", "10-inch tablet", new BigDecimal("299.99"), 10);
		productRepository.save(newProduct);

		List<Product> result = productRepository.findByNameContaining("Tablet");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Tablet", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findProductsWithNumericCharactersInSearch() {
		List<Product> result = productRepository.findByNameContaining("27");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Monitor", result.get(0).getName());
		assertTrue(result.get(0).getDescription().contains("27"));
	}

	@Test
	@Tag("valid")
	void findProductsWithHyphenatedSearchTerm() {
		List<Product> result = productRepository.findByNameContaining("27-inch");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Monitor", result.get(0).getName());
		assertTrue(result.get(0).getDescription().contains("27-inch"));
	}

	@Test
	@Tag("boundary")
	void findProductsInEmptyRepository() {
		for (long i = 1; i <= 5; i++) {
			productRepository.deleteById(i);
		}

		List<Product> result = productRepository.findByNameContaining("Laptop");

		assertNotNull(result);
		assertEquals(0, result.size());
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("boundary")
	void findProductsWithVeryLongSearchTerm() {
		String longSearchTerm = "a".repeat(150);

		List<Product> result = productRepository.findByNameContaining(longSearchTerm);

		assertNotNull(result);
		assertEquals(0, result.size());
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("valid")
	void findProductsWithSubstringAtBeginningOfName() {
		List<Product> result = productRepository.findByNameContaining("Head");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Headphones", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findProductsWithSubstringAtEndOfName() {
		List<Product> result = productRepository.findByNameContaining("board");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Keyboard", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findProductsWithSubstringInMiddleOfName() {
		List<Product> result = productRepository.findByNameContaining("onit");

		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Monitor", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findProductsWithRepeatedSearchCalls() {
		List<Product> firstResult = productRepository.findByNameContaining("Mouse");
		List<Product> secondResult = productRepository.findByNameContaining("Mouse");

		assertNotNull(firstResult);
		assertNotNull(secondResult);
		assertEquals(firstResult.size(), secondResult.size());
		assertEquals(1, firstResult.size());
		assertEquals(firstResult.get(0).getName(), secondResult.get(0).getName());
		assertEquals(firstResult.get(0).getId(), secondResult.get(0).getId());
	}

}