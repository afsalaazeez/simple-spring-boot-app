
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-simple-spring-boot-app using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=findByNameContaining_255b34584f
ROOST_METHOD_SIG_HASH=findByNameContaining_739e7ba865

Scenario 1: Find Products by Exact Name Match (Case Insensitive)

Details:
  TestName: findProductsByExactNameMatchCaseInsensitive
  Description: Verify that the method returns products when the search term exactly matches a product name, regardless of case differences.

Execution:
  Arrange: Use the default ProductRepository which is initialized with sample products including "Laptop", "Mouse", "Keyboard", "Monitor", and "Headphones".
  Act: Invoke findByNameContaining with the parameter "laptop" (lowercase).
  Assert: Verify that the returned list contains exactly one product and that product's name is "Laptop".

Validation:
  The assertion verifies that the case-insensitive search correctly matches "laptop" to "Laptop". This is important for user-friendly search functionality where users should not need to worry about exact capitalization.

---

Scenario 2: Find Products by Partial Name Match

Details:
  TestName: findProductsByPartialNameMatch
  Description: Verify that the method returns products when the search term is a substring of the product name.

Execution:
  Arrange: Use the default ProductRepository with pre-initialized sample products.
  Act: Invoke findByNameContaining with the parameter "board" (partial match for "Keyboard").
  Assert: Verify that the returned list contains exactly one product with the name "Keyboard".

Validation:
  The assertion confirms that partial string matching works correctly. This enables users to find products without knowing the complete product name, enhancing search usability.

---

Scenario 3: Find Multiple Products Matching Search Term

Details:
  TestName: findMultipleProductsMatchingSearchTerm
  Description: Verify that the method returns all products that contain the search term in their names.

Execution:
  Arrange: Save additional products to the repository, such as "Gaming Mouse" and "Mouse Pad", in addition to the existing "Mouse".
  Act: Invoke findByNameContaining with the parameter "Mouse".
  Assert: Verify that the returned list contains all products with "Mouse" in their name (at least 3 products).

Validation:
  The assertion ensures that the method returns all matching products, not just the first match. This is critical for comprehensive search results in e-commerce applications.

---

Scenario 4: Find Products with Empty Search String

Details:
  TestName: findProductsWithEmptySearchString
  Description: Verify that the method returns all products when an empty string is provided as the search term.

Execution:
  Arrange: Use the default ProductRepository with 5 pre-initialized sample products.
  Act: Invoke findByNameContaining with an empty string "".
  Assert: Verify that the returned list contains all 5 products.

Validation:
  The assertion confirms that an empty string matches all product names since every string contains an empty string. This behavior should be understood for proper UI handling.

---

Scenario 5: Find Products with No Matching Results

Details:
  TestName: findProductsWithNoMatchingResults
  Description: Verify that the method returns an empty list when no products match the search term.

Execution:
  Arrange: Use the default ProductRepository with pre-initialized sample products.
  Act: Invoke findByNameContaining with the parameter "Smartphone".
  Assert: Verify that the returned list is empty.

Validation:
  The assertion ensures that the method gracefully handles cases where no products match, returning an empty list rather than null. This prevents NullPointerException in calling code.

---

Scenario 6: Find Products with Mixed Case Search Term

Details:
  TestName: findProductsWithMixedCaseSearchTerm
  Description: Verify that the method correctly handles search terms with mixed uppercase and lowercase characters.

Execution:
  Arrange: Use the default ProductRepository with pre-initialized sample products.
  Act: Invoke findByNameContaining with the parameter "MoNiToR".
  Assert: Verify that the returned list contains exactly one product with the name "Monitor".

Validation:
  The assertion verifies that the toLowerCase() conversion is applied to both the search term and product names, ensuring consistent case-insensitive matching regardless of input format.

---

Scenario 7: Find Products with Single Character Search Term

Details:
  TestName: findProductsWithSingleCharacterSearchTerm
  Description: Verify that the method correctly handles a single character as the search term.

Execution:
  Arrange: Use the default ProductRepository with pre-initialized sample products.
  Act: Invoke findByNameContaining with the parameter "o".
  Assert: Verify that the returned list contains products whose names contain the letter "o" (e.g., "Mouse", "Keyboard", "Monitor", "Headphones").

Validation:
  The assertion confirms that single-character searches work correctly and return all products containing that character. This tests the minimum viable search input.

---

Scenario 8: Find Products with Special Characters in Search Term

Details:
  TestName: findProductsWithSpecialCharactersInSearchTerm
  Description: Verify that the method handles search terms containing special characters without errors.

Execution:
  Arrange: Save a new product with a name containing special characters, such as "USB-C Cable".
  Act: Invoke findByNameContaining with the parameter "-C".
  Assert: Verify that the returned list contains the product "USB-C Cable".

Validation:
  The assertion ensures that special characters in search terms are handled correctly without causing regex or parsing errors. This is important for products with technical naming conventions.

---

Scenario 9: Find Products with Numeric Characters in Search Term

Details:
  TestName: findProductsWithNumericCharactersInSearchTerm
  Description: Verify that the method correctly handles search terms containing numeric characters.

Execution:
  Arrange: Use the default ProductRepository which includes "27-inch 4K monitor" in the Monitor description (note: searching by name, so save a product like "4K TV").
  Act: Save a product named "4K TV" and invoke findByNameContaining with the parameter "4K".
  Assert: Verify that the returned list contains the product "4K TV".

Validation:
  The assertion confirms that numeric characters in product names are searchable. This is essential for products with model numbers or specifications in their names.

---

Scenario 10: Find Products with Whitespace in Search Term

Details:
  TestName: findProductsWithWhitespaceInSearchTerm
  Description: Verify that the method correctly handles search terms containing whitespace.

Execution:
  Arrange: Save a product with a multi-word name such as "Gaming Laptop Pro".
  Act: Invoke findByNameContaining with the parameter "Gaming Laptop".
  Assert: Verify that the returned list contains the product "Gaming Laptop Pro".

Validation:
  The assertion ensures that multi-word search terms with spaces are handled correctly. This supports natural language search queries from users.

---

Scenario 11: Find Products After Adding New Products

Details:
  TestName: findProductsAfterAddingNewProducts
  Description: Verify that newly added products are included in search results.

Execution:
  Arrange: Save a new product named "Tablet" to the repository.
  Act: Invoke findByNameContaining with the parameter "Tablet".
  Assert: Verify that the returned list contains exactly one product with the name "Tablet".

Validation:
  The assertion confirms that the ConcurrentHashMap storage is correctly updated and new products are immediately searchable. This validates the real-time nature of the in-memory repository.

---

Scenario 12: Find Products After Deleting Products

Details:
  TestName: findProductsAfterDeletingProducts
  Description: Verify that deleted products are not included in search results.

Execution:
  Arrange: Use the default ProductRepository, find the ID of "Mouse" product, and delete it using deleteById.
  Act: Invoke findByNameContaining with the parameter "Mouse".
  Assert: Verify that the returned list is empty.

Validation:
  The assertion ensures that deleted products are properly removed from the search index. This is critical for maintaining data consistency in the application.

---

Scenario 13: Find Products with Leading and Trailing Spaces in Search Term

Details:
  TestName: findProductsWithLeadingAndTrailingSpacesInSearchTerm
  Description: Verify how the method handles search terms with leading and trailing whitespace.

Execution:
  Arrange: Use the default ProductRepository with pre-initialized sample products.
  Act: Invoke findByNameContaining with the parameter "  Laptop  " (with spaces).
  Assert: Verify that the returned list is empty (since product names don't contain leading/trailing spaces).

Validation:
  The assertion clarifies the behavior when search terms include extra whitespace. This helps developers understand whether input trimming should be done before calling this method.

---

Scenario 14: Find Products from Empty Repository

Details:
  TestName: findProductsFromEmptyRepository
  Description: Verify that the method returns an empty list when the repository contains no products.

Execution:
  Arrange: Create a new ProductRepository and delete all pre-initialized products using deleteById for each product.
  Act: Invoke findByNameContaining with the parameter "Laptop".
  Assert: Verify that the returned list is empty.

Validation:
  The assertion ensures the method handles an empty repository gracefully without throwing exceptions. This is an important edge case for newly initialized or cleared repositories.

---

Scenario 15: Verify Returned List is Immutable

Details:
  TestName: verifyReturnedListIsImmutable
  Description: Verify that the list returned by findByNameContaining is immutable (as toList() returns an unmodifiable list).

Execution:
  Arrange: Use the default ProductRepository with pre-initialized sample products.
  Act: Invoke findByNameContaining with the parameter "Laptop" and attempt to add a new product to the returned list.
  Assert: Verify that an UnsupportedOperationException is thrown when attempting to modify the returned list.

Validation:
  The assertion confirms that the returned list cannot be modified, which protects the internal state of the repository from unintended modifications through the returned collection.

*/

// ********RoostGPT********

package com.example.demo.repository;

import com.example.demo.model.Product;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import java.math.BigDecimal;
import java.util.List;
import java.util.Optional;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.stereotype.Repository;
import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicLong;

class ProductRepositoryFindByNameContainingTest {

	private ProductRepository productRepository;

	@BeforeEach
	void setUp() {
		productRepository = new ProductRepository();
	}

	@Test
	@Tag("valid")
	void findProductsByExactNameMatchCaseInsensitive() {
		// Arrange: Use the default ProductRepository which is initialized with sample
		// products
		// Act
		List<Product> result = productRepository.findByNameContaining("laptop");
		// Assert
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Laptop", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findProductsByPartialNameMatch() {
		// Arrange: Use the default ProductRepository with pre-initialized sample products
		// Act
		List<Product> result = productRepository.findByNameContaining("board");
		// Assert
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Keyboard", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findMultipleProductsMatchingSearchTerm() {
		// Arrange: Save additional products
		productRepository.save(new Product("Gaming Mouse", "High-precision gaming mouse", new BigDecimal("59.99"), 40));
		productRepository.save(new Product("Mouse Pad", "Large gaming mouse pad", new BigDecimal("19.99"), 100));
		// Act
		List<Product> result = productRepository.findByNameContaining("Mouse");
		// Assert
		assertNotNull(result);
		assertTrue(result.size() >= 3);
		assertTrue(result.stream().allMatch(p -> p.getName().toLowerCase().contains("mouse")));
	}

	@Test
	@Tag("boundary")
	void findProductsWithEmptySearchString() {
		// Arrange: Use the default ProductRepository with 5 pre-initialized sample
		// products
		// Act
		List<Product> result = productRepository.findByNameContaining("");
		// Assert
		assertNotNull(result);
		assertEquals(5, result.size());
	}

	@Test
	@Tag("valid")
	void findProductsWithNoMatchingResults() {
		// Arrange: Use the default ProductRepository with pre-initialized sample products
		// Act
		List<Product> result = productRepository.findByNameContaining("Smartphone");
		// Assert
		assertNotNull(result);
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("valid")
	void findProductsWithMixedCaseSearchTerm() {
		// Arrange: Use the default ProductRepository with pre-initialized sample products
		// Act
		List<Product> result = productRepository.findByNameContaining("MoNiToR");
		// Assert
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Monitor", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findProductsWithSingleCharacterSearchTerm() {
		// Arrange: Use the default ProductRepository with pre-initialized sample products
		// Act
		List<Product> result = productRepository.findByNameContaining("o");
		// Assert
		assertNotNull(result);
		assertFalse(result.isEmpty());
		assertTrue(result.stream().allMatch(p -> p.getName().toLowerCase().contains("o")));
		// Expected products: Mouse, Keyboard, Monitor, Headphones
		assertTrue(result.size() >= 4);
	}

	@Test
	@Tag("valid")
	void findProductsWithSpecialCharactersInSearchTerm() {
		// Arrange: Save a new product with a name containing special characters
		productRepository.save(new Product("USB-C Cable", "Fast charging cable", new BigDecimal("14.99"), 200));
		// Act
		List<Product> result = productRepository.findByNameContaining("-C");
		// Assert
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("USB-C Cable", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findProductsWithNumericCharactersInSearchTerm() {
		// Arrange: Save a product with numeric characters in name
		productRepository.save(new Product("4K TV", "Ultra HD television", new BigDecimal("599.99"), 10));
		// Act
		List<Product> result = productRepository.findByNameContaining("4K");
		// Assert
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("4K TV", result.get(0).getName());
	}

	@Test
	@Tag("valid")
	void findProductsWithWhitespaceInSearchTerm() {
		// Arrange: Save a product with a multi-word name
		productRepository
			.save(new Product("Gaming Laptop Pro", "High-end gaming laptop", new BigDecimal("1999.99"), 5));
		// Act
		List<Product> result = productRepository.findByNameContaining("Gaming Laptop");
		// Assert
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Gaming Laptop Pro", result.get(0).getName());
	}

	@Test
	@Tag("integration")
	void findProductsAfterAddingNewProducts() {
		// Arrange: Save a new product named "Tablet"
		productRepository.save(new Product("Tablet", "10-inch tablet", new BigDecimal("349.99"), 35));
		// Act
		List<Product> result = productRepository.findByNameContaining("Tablet");
		// Assert
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Tablet", result.get(0).getName());
	}

	@Test
	@Tag("integration")
	void findProductsAfterDeletingProducts() {
		// Arrange: Find the ID of "Mouse" product and delete it
		List<Product> mouseProducts = productRepository.findByNameContaining("Mouse");
		assertFalse(mouseProducts.isEmpty());
		Long mouseId = mouseProducts.get(0).getId();
		productRepository.deleteById(mouseId);
		// Act
		List<Product> result = productRepository.findByNameContaining("Mouse");
		// Assert
		assertNotNull(result);
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("boundary")
	void findProductsWithLeadingAndTrailingSpacesInSearchTerm() {
		// Arrange: Use the default ProductRepository with pre-initialized sample products
		// Act
		List<Product> result = productRepository.findByNameContaining("  Laptop  ");
		// Assert
		assertNotNull(result);
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("boundary")
	void findProductsFromEmptyRepository() {
		// Arrange: Delete all pre-initialized products
		List<Product> allProducts = productRepository.findAll();
		for (Product product : allProducts) {
			productRepository.deleteById(product.getId());
		}
		// Act
		List<Product> result = productRepository.findByNameContaining("Laptop");
		// Assert
		assertNotNull(result);
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("valid")
	void verifyReturnedListIsImmutable() {
		// Arrange: Use the default ProductRepository with pre-initialized sample products
		// Act
		List<Product> result = productRepository.findByNameContaining("Laptop");
		// Assert
		assertNotNull(result);
		assertFalse(result.isEmpty());
		assertThrows(UnsupportedOperationException.class, () -> {
			result.add(new Product("New Product", "Description", new BigDecimal("99.99"), 10));
		});
	}

}