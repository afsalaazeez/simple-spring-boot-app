
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-simple-spring-boot-app using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=findByPriceRange_a4910d76fa
ROOST_METHOD_SIG_HASH=findByPriceRange_30ce6d6836

Scenario 1: Find Products Within Valid Price Range

Details:
  TestName: findProductsWithinValidPriceRange
  Description: This test verifies that the findByPriceRange method correctly returns all products whose prices fall within the specified minimum and maximum price range. It checks the core functionality of the method with a typical use case where multiple products exist within the given range.

Execution:
  Arrange: Initialize the ProductRepository which contains sample products with various prices (Laptop: 999.99, Mouse: 29.99, Keyboard: 89.99, Monitor: 399.99, Headphones: 199.99). Set minPrice to 50.00 and maxPrice to 500.00.
  Act: Call findByPriceRange(new BigDecimal("50.00"), new BigDecimal("500.00")) on the repository instance.
  Assert: Verify that the returned list contains exactly 3 products (Keyboard: 89.99, Monitor: 399.99, Headphones: 199.99). Assert that the list size is 3 and each product's price is within the specified range.

Validation:
  This assertion verifies that the method correctly filters products based on the price range criteria using compareTo method with >= and <= operators. The test ensures that only products with prices between 50.00 and 500.00 (inclusive) are returned, which is critical for e-commerce filtering functionality where customers search for products within their budget.

---

Scenario 2: Find Products With Exact Minimum Price Match

Details:
  TestName: findProductsWithExactMinimumPrice
  Description: This test validates that products with prices exactly equal to the minimum price are included in the results. It ensures the >= comparison operator is working correctly for the lower boundary.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to 29.99 (exact price of Mouse) and maxPrice to 100.00.
  Act: Call findByPriceRange(new BigDecimal("29.99"), new BigDecimal("100.00")) on the repository instance.
  Assert: Verify that the returned list contains Mouse (29.99) and Keyboard (89.99). Assert that the list size is 2 and the Mouse product is included in the results.

Validation:
  This assertion confirms that the >= operator in the filter condition correctly includes products with prices exactly matching the minimum price. This is important for boundary testing to ensure customers don't miss products at the exact lower price limit of their search.

---

Scenario 3: Find Products With Exact Maximum Price Match

Details:
  TestName: findProductsWithExactMaximumPrice
  Description: This test ensures that products with prices exactly equal to the maximum price are included in the results. It validates the <= comparison operator for the upper boundary.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to 100.00 and maxPrice to 399.99 (exact price of Monitor).
  Act: Call findByPriceRange(new BigDecimal("100.00"), new BigDecimal("399.99")) on the repository instance.
  Assert: Verify that the returned list contains Headphones (199.99) and Monitor (399.99). Assert that the list size is 2 and the Monitor product is included in the results.

Validation:
  This assertion validates that the <= operator in the filter condition correctly includes products with prices exactly matching the maximum price. This boundary test ensures the inclusive nature of the upper limit, which is essential for accurate product filtering.

---

Scenario 4: Find Products With Identical Min and Max Price

Details:
  TestName: findProductsWithIdenticalMinAndMaxPrice
  Description: This test checks the edge case where minPrice and maxPrice are identical, effectively searching for products with an exact price match.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set both minPrice and maxPrice to 89.99 (exact price of Keyboard).
  Act: Call findByPriceRange(new BigDecimal("89.99"), new BigDecimal("89.99")) on the repository instance.
  Assert: Verify that the returned list contains exactly one product (Keyboard with price 89.99). Assert that the list size is 1.

Validation:
  This assertion verifies that the method handles the edge case where the price range has zero width. The combination of >= and <= operators should correctly return products with the exact price specified. This is useful for precise price searches in the application.

---

Scenario 5: Find Products With No Matches in Price Range

Details:
  TestName: findProductsWithNoMatchesInPriceRange
  Description: This test validates that the method returns an empty list when no products exist within the specified price range.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to 1000.00 and maxPrice to 2000.00 (range where no products exist).
  Act: Call findByPriceRange(new BigDecimal("1000.00"), new BigDecimal("2000.00")) on the repository instance.
  Assert: Verify that the returned list is empty. Assert that the list size is 0 and the list is not null.

Validation:
  This assertion confirms that the method gracefully handles scenarios where no products match the criteria, returning an empty list rather than null or throwing an exception. This is important for user experience, allowing the application to display "no results found" messages appropriately.

---

Scenario 6: Find All Products With Very Wide Price Range

Details:
  TestName: findAllProductsWithVeryWidePriceRange
  Description: This test verifies that when the price range encompasses all products, all products are returned.

Execution:
  Arrange: Use the ProductRepository with initialized sample data containing 5 products. Set minPrice to 0.00 and maxPrice to 10000.00 (range covering all products).
  Act: Call findByPriceRange(new BigDecimal("0.00"), new BigDecimal("10000.00")) on the repository instance.
  Assert: Verify that the returned list contains all 5 products. Assert that the list size is 5.

Validation:
  This assertion validates that the method correctly returns all products when the price range is sufficiently wide. This test ensures the filter logic doesn't inadvertently exclude valid products and that the method can handle broad search criteria.

---

Scenario 7: Find Products With Minimum Price Zero

Details:
  TestName: findProductsWithMinimumPriceZero
  Description: This test checks the behavior when the minimum price is set to zero, which is a valid edge case for free or promotional items.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to 0.00 and maxPrice to 50.00.
  Act: Call findByPriceRange(new BigDecimal("0.00"), new BigDecimal("50.00")) on the repository instance.
  Assert: Verify that the returned list contains only the Mouse (29.99). Assert that the list size is 1.

Validation:
  This assertion ensures that the method correctly handles zero as a minimum price, which is important for applications that may have free products or promotional items. The >= comparison should work correctly with zero values.

---

Scenario 8: Find Products With Very Small Price Range

Details:
  TestName: findProductsWithVerySmallPriceRange
  Description: This test validates the method's behavior with a very narrow price range that may or may not contain products.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to 29.00 and maxPrice to 30.00 (narrow range around Mouse price).
  Act: Call findByPriceRange(new BigDecimal("29.00"), new BigDecimal("30.00")) on the repository instance.
  Assert: Verify that the returned list contains the Mouse (29.99). Assert that the list size is 1.

Validation:
  This assertion confirms that the method can accurately filter products even with very narrow price ranges. This is important for precision in price filtering and ensures the compareTo method works correctly with small decimal differences.

---

Scenario 9: Find Products With Decimal Precision in Price Range

Details:
  TestName: findProductsWithDecimalPrecisionInPriceRange
  Description: This test ensures that the method correctly handles BigDecimal precision when comparing prices with multiple decimal places.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to 199.99 (exact Headphones price) and maxPrice to 399.99 (exact Monitor price).
  Act: Call findByPriceRange(new BigDecimal("199.99"), new BigDecimal("399.99")) on the repository instance.
  Assert: Verify that the returned list contains Headphones (199.99) and Monitor (399.99). Assert that the list size is 2 and both boundary products are included.

Validation:
  This assertion validates that BigDecimal comparison works correctly with precise decimal values. Since BigDecimal is used for monetary values to avoid floating-point precision issues, this test ensures the compareTo method maintains accuracy at the boundaries.

---

Scenario 10: Find Products With Price Range Excluding All Products

Details:
  TestName: findProductsWithPriceRangeExcludingAllProducts
  Description: This test verifies the method returns an empty list when the price range is below all product prices.

Execution:
  Arrange: Use the ProductRepository with initialized sample data where the minimum product price is 29.99. Set minPrice to 0.01 and maxPrice to 10.00.
  Act: Call findByPriceRange(new BigDecimal("0.01"), new BigDecimal("10.00")) on the repository instance.
  Assert: Verify that the returned list is empty. Assert that the list size is 0.

Validation:
  This assertion ensures the method correctly returns an empty list when the specified range is entirely below the available product prices. This validates the lower boundary filtering logic and ensures no false positives are returned.

---

Scenario 11: Find Products With Price Range Between Two Products

Details:
  TestName: findProductsWithPriceRangeBetweenTwoProducts
  Description: This test validates that the method returns an empty list when the price range falls between two product prices without including either.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to 30.00 and maxPrice to 89.00 (between Mouse at 29.99 and Keyboard at 89.99).
  Act: Call findByPriceRange(new BigDecimal("30.00"), new BigDecimal("89.00")) on the repository instance.
  Assert: Verify that the returned list is empty. Assert that the list size is 0.

Validation:
  This assertion confirms that the method correctly excludes products when their prices fall outside the specified range, even by small margins. This tests the precision of the >= and <= operators and ensures no products are incorrectly included.

---

Scenario 12: Find Products With Maximum Price as Large Value

Details:
  TestName: findProductsWithMaximumPriceAsLargeValue
  Description: This test checks the method's behavior when the maximum price is set to a very large value, effectively removing the upper limit.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to 500.00 and maxPrice to 999999.99 (very large value).
  Act: Call findByPriceRange(new BigDecimal("500.00"), new BigDecimal("999999.99")) on the repository instance.
  Assert: Verify that the returned list contains only the Laptop (999.99). Assert that the list size is 1.

Validation:
  This assertion validates that the method can handle very large maximum price values without issues. This is important for scenarios where users want to see all products above a certain price without specifying an upper limit.

---

Scenario 13: Find Products With Empty Repository

Details:
  TestName: findProductsWithEmptyRepository
  Description: This test verifies the method's behavior when the products map is empty (after clearing all products).

Execution:
  Arrange: Create a new ProductRepository instance and delete all existing products using deleteById for each product ID, resulting in an empty products map. Set minPrice to 0.00 and maxPrice to 1000.00.
  Act: Call findByPriceRange(new BigDecimal("0.00"), new BigDecimal("1000.00")) on the empty repository instance.
  Assert: Verify that the returned list is empty. Assert that the list size is 0 and the list is not null.

Validation:
  This assertion ensures the method handles an empty repository gracefully without throwing exceptions. This is important for robustness, especially during application initialization or after bulk deletions.

---

Scenario 14: Find Products With Multiple Products at Same Price

Details:
  TestName: findProductsWithMultipleProductsAtSamePrice
  Description: This test validates the method's behavior when multiple products have identical prices within the search range.

Execution:
  Arrange: Create a ProductRepository and add additional products with the same price (e.g., two products at 100.00). Set minPrice to 50.00 and maxPrice to 150.00.
  Act: Call findByPriceRange(new BigDecimal("50.00"), new BigDecimal("150.00")) on the repository instance.
  Assert: Verify that the returned list contains all products with prices in the range, including multiple products with identical prices. Assert that duplicate prices are handled correctly.

Validation:
  This assertion confirms that the method correctly returns all matching products regardless of price duplication. This is important for inventory systems where multiple products may have the same price point.

---

Scenario 15: Find Products With Inverted Price Range (Min Greater Than Max)

Details:
  TestName: findProductsWithInvertedPriceRange
  Description: This test examines the method's behavior when minPrice is greater than maxPrice, which is a logical error in the input parameters.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to 500.00 and maxPrice to 100.00 (inverted range).
  Act: Call findByPriceRange(new BigDecimal("500.00"), new BigDecimal("100.00")) on the repository instance.
  Assert: Verify that the returned list is empty. Assert that the list size is 0, as no product can satisfy the condition where price >= 500 AND price <= 100.

Validation:
  This assertion validates that the method logically handles invalid input where the minimum exceeds the maximum. The filter condition (price >= min AND price <= max) will naturally return no results, which is the expected behavior. This test highlights the importance of input validation at the service or controller layer.

---

Scenario 16: Find Products With Negative Price Values

Details:
  TestName: findProductsWithNegativePriceValues
  Description: This test checks the method's behavior when negative values are provided for price range, which should not match any products with positive prices.

Execution:
  Arrange: Use the ProductRepository with initialized sample data (all products have positive prices). Set minPrice to -100.00 and maxPrice to -10.00.
  Act: Call findByPriceRange(new BigDecimal("-100.00"), new BigDecimal("-10.00")) on the repository instance.
  Assert: Verify that the returned list is empty. Assert that the list size is 0.

Validation:
  This assertion ensures the method correctly handles negative price values in the search criteria. Since all products have positive prices, no matches should be found. This test validates the robustness of the comparison logic with edge case inputs.

---

Scenario 17: Find Products With One Negative and One Positive Price

Details:
  TestName: findProductsWithOneNegativeAndOnePositivePrice
  Description: This test validates the method when the price range spans from negative to positive values.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to -50.00 and maxPrice to 50.00.
  Act: Call findByPriceRange(new BigDecimal("-50.00"), new BigDecimal("50.00")) on the repository instance.
  Assert: Verify that the returned list contains only the Mouse (29.99). Assert that the list size is 1.

Validation:
  This assertion confirms that the method correctly filters products when the range includes both negative and positive values. Products with prices >= -50 and <= 50 should be returned, demonstrating the method's flexibility with various input ranges.

---

Scenario 18: Find Products After Adding New Product in Range

Details:
  TestName: findProductsAfterAddingNewProductInRange
  Description: This test verifies that the method correctly includes newly added products that fall within the specified price range.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Add a new product with price 150.00 using the save method. Set minPrice to 100.00 and maxPrice to 200.00.
  Act: Call findByPriceRange(new BigDecimal("100.00"), new BigDecimal("200.00")) on the repository instance.
  Assert: Verify that the returned list contains both the newly added product (150.00) and Headphones (199.99). Assert that the list size is 2.

Validation:
  This assertion validates that the method dynamically reflects changes to the products map, including newly added products. This is important for real-time inventory systems where products are continuously added and the search results must be current.

---

Scenario 19: Find Products With BigDecimal Scale Differences

Details:
  TestName: findProductsWithBigDecimalScaleDifferences
  Description: This test ensures that BigDecimal values with different scales but equal values are compared correctly.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. Set minPrice to 29.990 (scale 3) and maxPrice to 200.0 (scale 1).
  Act: Call findByPriceRange(new BigDecimal("29.990"), new BigDecimal("200.0")) on the repository instance.
  Assert: Verify that the returned list contains Mouse (29.99), Keyboard (89.99), and Headphones (199.99). Assert that the list size is 3 and BigDecimal comparison handles different scales correctly.

Validation:
  This assertion confirms that BigDecimal's compareTo method correctly handles values with different scales. This is crucial for monetary calculations where 29.99, 29.990, and 29.9900 should be treated as equal values.

---

Scenario 20: Find Products With Concurrent Modifications

Details:
  TestName: findProductsWithConcurrentModifications
  Description: This test validates that the method works correctly with the ConcurrentHashMap when products are being modified during the search operation.

Execution:
  Arrange: Use the ProductRepository with initialized sample data. The products map is a ConcurrentHashMap which supports concurrent operations. Set minPrice to 0.00 and maxPrice to 1000.00.
  Act: Call findByPriceRange(new BigDecimal("0.00"), new BigDecimal("1000.00")) on the repository instance while the underlying map may be accessed concurrently.
  Assert: Verify that the returned list contains all matching products without throwing ConcurrentModificationException. Assert that the operation completes successfully.

Validation:
  This assertion ensures that the stream operation on ConcurrentHashMap.values() is thread-safe and doesn't throw exceptions during concurrent access. This is important for multi-threaded applications where multiple users may be searching products simultaneously while inventory is being updated.

*/

// ********RoostGPT********

package com.example.demo.repository;

import com.example.demo.model.Product;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import java.math.BigDecimal;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.stereotype.Repository;
import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicLong;

class ProductRepositoryFindByPriceRangeTest {

	private ProductRepository productRepository;

	@BeforeEach
	void setUp() {
		productRepository = new ProductRepository();
	}

	@Test
	@Tag("valid")
	void findProductsWithinValidPriceRange() {
		BigDecimal minPrice = new BigDecimal("50.00");
		BigDecimal maxPrice = new BigDecimal("500.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(3, result.size());
		assertTrue(result.stream()
			.allMatch(product -> product.getPrice().compareTo(minPrice) >= 0
					&& product.getPrice().compareTo(maxPrice) <= 0));
		assertTrue(result.stream().anyMatch(product -> product.getName().equals("Keyboard")));
		assertTrue(result.stream().anyMatch(product -> product.getName().equals("Monitor")));
		assertTrue(result.stream().anyMatch(product -> product.getName().equals("Headphones")));
	}

	@Test
	@Tag("boundary")
	void findProductsWithExactMinimumPrice() {
		BigDecimal minPrice = new BigDecimal("29.99");
		BigDecimal maxPrice = new BigDecimal("100.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(2, result.size());
		assertTrue(result.stream()
			.anyMatch(product -> product.getName().equals("Mouse")
					&& product.getPrice().compareTo(new BigDecimal("29.99")) == 0));
		assertTrue(result.stream().anyMatch(product -> product.getName().equals("Keyboard")));
	}

	@Test
	@Tag("boundary")
	void findProductsWithExactMaximumPrice() {
		BigDecimal minPrice = new BigDecimal("100.00");
		BigDecimal maxPrice = new BigDecimal("399.99");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(2, result.size());
		assertTrue(result.stream().anyMatch(product -> product.getName().equals("Headphones")));
		assertTrue(result.stream()
			.anyMatch(product -> product.getName().equals("Monitor")
					&& product.getPrice().compareTo(new BigDecimal("399.99")) == 0));
	}

	@Test
	@Tag("boundary")
	void findProductsWithIdenticalMinAndMaxPrice() {
		BigDecimal price = new BigDecimal("89.99");
		List<Product> result = productRepository.findByPriceRange(price, price);
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Keyboard", result.get(0).getName());
		assertEquals(0, result.get(0).getPrice().compareTo(new BigDecimal("89.99")));
	}

	@Test
	@Tag("valid")
	void findProductsWithNoMatchesInPriceRange() {
		BigDecimal minPrice = new BigDecimal("1000.00");
		BigDecimal maxPrice = new BigDecimal("2000.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertTrue(result.isEmpty());
		assertEquals(0, result.size());
	}

	@Test
	@Tag("valid")
	void findAllProductsWithVeryWidePriceRange() {
		BigDecimal minPrice = new BigDecimal("0.00");
		BigDecimal maxPrice = new BigDecimal("10000.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(5, result.size());
	}

	@Test
	@Tag("boundary")
	void findProductsWithMinimumPriceZero() {
		BigDecimal minPrice = new BigDecimal("0.00");
		BigDecimal maxPrice = new BigDecimal("50.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Mouse", result.get(0).getName());
		assertEquals(0, result.get(0).getPrice().compareTo(new BigDecimal("29.99")));
	}

	@Test
	@Tag("valid")
	void findProductsWithVerySmallPriceRange() {
		BigDecimal minPrice = new BigDecimal("29.00");
		BigDecimal maxPrice = new BigDecimal("30.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Mouse", result.get(0).getName());
		assertEquals(0, result.get(0).getPrice().compareTo(new BigDecimal("29.99")));
	}

	@Test
	@Tag("valid")
	void findProductsWithDecimalPrecisionInPriceRange() {
		BigDecimal minPrice = new BigDecimal("199.99");
		BigDecimal maxPrice = new BigDecimal("399.99");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(2, result.size());
		assertTrue(result.stream()
			.anyMatch(product -> product.getName().equals("Headphones")
					&& product.getPrice().compareTo(new BigDecimal("199.99")) == 0));
		assertTrue(result.stream()
			.anyMatch(product -> product.getName().equals("Monitor")
					&& product.getPrice().compareTo(new BigDecimal("399.99")) == 0));
	}

	@Test
	@Tag("valid")
	void findProductsWithPriceRangeExcludingAllProducts() {
		BigDecimal minPrice = new BigDecimal("0.01");
		BigDecimal maxPrice = new BigDecimal("10.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertTrue(result.isEmpty());
		assertEquals(0, result.size());
	}

	@Test
	@Tag("boundary")
	void findProductsWithPriceRangeBetweenTwoProducts() {
		BigDecimal minPrice = new BigDecimal("30.00");
		BigDecimal maxPrice = new BigDecimal("89.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertTrue(result.isEmpty());
		assertEquals(0, result.size());
	}

	@Test
	@Tag("valid")
	void findProductsWithMaximumPriceAsLargeValue() {
		BigDecimal minPrice = new BigDecimal("500.00");
		BigDecimal maxPrice = new BigDecimal("999999.99");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Laptop", result.get(0).getName());
		assertEquals(0, result.get(0).getPrice().compareTo(new BigDecimal("999.99")));
	}

	@Test
	@Tag("valid")
	void findProductsWithEmptyRepository() {
		ProductRepository emptyRepository = new ProductRepository();
		List<Product> allProducts = emptyRepository.findAll();
		for (Product product : allProducts) {
			emptyRepository.deleteById(product.getId());
		}
		BigDecimal minPrice = new BigDecimal("0.00");
		BigDecimal maxPrice = new BigDecimal("1000.00");
		List<Product> result = emptyRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertTrue(result.isEmpty());
		assertEquals(0, result.size());
	}

	@Test
	@Tag("valid")
	void findProductsWithMultipleProductsAtSamePrice() {
		Product product1 = new Product("Product1", "Description1", new BigDecimal("100.00"), 10);
		Product product2 = new Product("Product2", "Description2", new BigDecimal("100.00"), 20);
		productRepository.save(product1);
		productRepository.save(product2);
		BigDecimal minPrice = new BigDecimal("50.00");
		BigDecimal maxPrice = new BigDecimal("150.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertTrue(result.size() >= 2);
		long countAt100 = result.stream().filter(p -> p.getPrice().compareTo(new BigDecimal("100.00")) == 0).count();
		assertTrue(countAt100 >= 2);
	}

	@Test
	@Tag("invalid")
	void findProductsWithInvertedPriceRange() {
		BigDecimal minPrice = new BigDecimal("500.00");
		BigDecimal maxPrice = new BigDecimal("100.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertTrue(result.isEmpty());
		assertEquals(0, result.size());
	}

	@Test
	@Tag("boundary")
	void findProductsWithNegativePriceValues() {
		BigDecimal minPrice = new BigDecimal("-100.00");
		BigDecimal maxPrice = new BigDecimal("-10.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertTrue(result.isEmpty());
		assertEquals(0, result.size());
	}

	@Test
	@Tag("boundary")
	void findProductsWithOneNegativeAndOnePositivePrice() {
		BigDecimal minPrice = new BigDecimal("-50.00");
		BigDecimal maxPrice = new BigDecimal("50.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(1, result.size());
		assertEquals("Mouse", result.get(0).getName());
		assertEquals(0, result.get(0).getPrice().compareTo(new BigDecimal("29.99")));
	}

	@Test
	@Tag("integration")
	void findProductsAfterAddingNewProductInRange() {
		Product newProduct = new Product("NewProduct", "New Description", new BigDecimal("150.00"), 15);
		productRepository.save(newProduct);
		BigDecimal minPrice = new BigDecimal("100.00");
		BigDecimal maxPrice = new BigDecimal("200.00");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(2, result.size());
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("NewProduct")));
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Headphones")));
	}

	@Test
	@Tag("valid")
	void findProductsWithBigDecimalScaleDifferences() {
		BigDecimal minPrice = new BigDecimal("29.990");
		BigDecimal maxPrice = new BigDecimal("200.0");
		List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
		assertNotNull(result);
		assertEquals(3, result.size());
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Mouse")));
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Keyboard")));
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Headphones")));
	}

	@Test
	@Tag("integration")
	void findProductsWithConcurrentModifications() {
		BigDecimal minPrice = new BigDecimal("0.00");
		BigDecimal maxPrice = new BigDecimal("1000.00");
		assertDoesNotThrow(() -> {
			List<Product> result = productRepository.findByPriceRange(minPrice, maxPrice);
			assertNotNull(result);
			assertEquals(5, result.size());
		});
	}

}