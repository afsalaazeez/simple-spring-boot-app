
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-simple-spring-boot-app using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=findByPriceRange_a4910d76fa
ROOST_METHOD_SIG_HASH=findByPriceRange_30ce6d6836

Scenario 1: Find Products Within Valid Price Range

Details:
  TestName: findProductsWithinValidPriceRange
  Description: Verify that the method returns all products whose prices fall within the specified minimum and maximum price range (inclusive on both ends).

Execution:
  Arrange: Create a ProductRepository instance which initializes with sample data containing products at various price points (29.99, 89.99, 199.99, 399.99, 999.99).
  Act: Invoke findByPriceRange with minPrice = 50.00 and maxPrice = 250.00.
  Assert: Verify that the returned list contains exactly 2 products (Keyboard at 89.99 and Headphones at 199.99).

Validation:
  The assertion verifies that only products with prices >= 50.00 AND <= 250.00 are returned. This test confirms the basic filtering logic works correctly for a typical price range query, which is essential for e-commerce price filtering functionality.

---

Scenario 2: Find Products When Price Equals Minimum Boundary

Details:
  TestName: findProductsWhenPriceEqualsMinimumBoundary
  Description: Verify that products with prices exactly equal to the minimum price are included in the results (inclusive lower bound).

Execution:
  Arrange: Create a ProductRepository instance with default sample data.
  Act: Invoke findByPriceRange with minPrice = 29.99 and maxPrice = 100.00.
  Assert: Verify that the returned list includes the Mouse product (priced at exactly 29.99) and Keyboard (priced at 89.99).

Validation:
  The assertion confirms that the >= comparison operator correctly includes products at the exact minimum price boundary. This is critical for ensuring customers see all products at or above their minimum budget.

---

Scenario 3: Find Products When Price Equals Maximum Boundary

Details:
  TestName: findProductsWhenPriceEqualsMaximumBoundary
  Description: Verify that products with prices exactly equal to the maximum price are included in the results (inclusive upper bound).

Execution:
  Arrange: Create a ProductRepository instance with default sample data.
  Act: Invoke findByPriceRange with minPrice = 300.00 and maxPrice = 399.99.
  Assert: Verify that the returned list includes the Monitor product (priced at exactly 399.99).

Validation:
  The assertion confirms that the <= comparison operator correctly includes products at the exact maximum price boundary. This ensures customers can find products at their maximum budget limit.

---

Scenario 4: Find Products When Min And Max Price Are Equal

Details:
  TestName: findProductsWhenMinAndMaxPriceAreEqual
  Description: Verify that when minimum and maximum prices are the same, only products with that exact price are returned.

Execution:
  Arrange: Create a ProductRepository instance with default sample data.
  Act: Invoke findByPriceRange with minPrice = 199.99 and maxPrice = 199.99.
  Assert: Verify that the returned list contains exactly 1 product (Headphones at 199.99).

Validation:
  The assertion verifies that the method handles the edge case where the price range is a single value. This is useful for exact price matching scenarios.

---

Scenario 5: Find Products Returns Empty List When No Products In Range

Details:
  TestName: findProductsReturnsEmptyListWhenNoProductsInRange
  Description: Verify that an empty list is returned when no products fall within the specified price range.

Execution:
  Arrange: Create a ProductRepository instance with default sample data.
  Act: Invoke findByPriceRange with minPrice = 500.00 and maxPrice = 600.00.
  Assert: Verify that the returned list is empty (size = 0).

Validation:
  The assertion confirms that the method gracefully handles cases where no products match the criteria, returning an empty list rather than null. This prevents NullPointerException in calling code.

---

Scenario 6: Find Products With Very Wide Price Range Returns All Products

Details:
  TestName: findProductsWithVeryWidePriceRangeReturnsAllProducts
  Description: Verify that when the price range encompasses all product prices, all products are returned.

Execution:
  Arrange: Create a ProductRepository instance with default sample data containing 5 products.
  Act: Invoke findByPriceRange with minPrice = 0.00 and maxPrice = 10000.00.
  Assert: Verify that the returned list contains all 5 products.

Validation:
  The assertion confirms that a wide price range correctly captures all products in the repository. This validates the filter logic works across the entire price spectrum.

---

Scenario 7: Find Products With Zero As Minimum Price

Details:
  TestName: findProductsWithZeroAsMinimumPrice
  Description: Verify that using zero as the minimum price correctly includes all products up to the maximum price.

Execution:
  Arrange: Create a ProductRepository instance with default sample data.
  Act: Invoke findByPriceRange with minPrice = BigDecimal.ZERO and maxPrice = 100.00.
  Assert: Verify that the returned list contains Mouse (29.99) and Keyboard (89.99).

Validation:
  The assertion verifies that BigDecimal.ZERO is handled correctly as a valid minimum price boundary. This is important for "up to X price" type queries.

---

Scenario 8: Find Products After Adding New Product In Range

Details:
  TestName: findProductsAfterAddingNewProductInRange
  Description: Verify that newly added products within the price range are included in subsequent queries.

Execution:
  Arrange: Create a ProductRepository instance and save a new Product with name "USB Cable", description "USB-C cable", price 15.99, and stock 100.
  Act: Invoke findByPriceRange with minPrice = 10.00 and maxPrice = 20.00.
  Assert: Verify that the returned list contains the newly added USB Cable product.

Validation:
  The assertion confirms that the ConcurrentHashMap-backed storage correctly reflects newly added products in query results. This validates the repository's data consistency.

---

Scenario 9: Find Products With High Precision Decimal Prices

Details:
  TestName: findProductsWithHighPrecisionDecimalPrices
  Description: Verify that BigDecimal comparison works correctly with high precision decimal values.

Execution:
  Arrange: Create a ProductRepository instance with default sample data.
  Act: Invoke findByPriceRange with minPrice = new BigDecimal("29.990") and maxPrice = new BigDecimal("29.990").
  Assert: Verify that the returned list contains the Mouse product (29.99 equals 29.990 in BigDecimal comparison).

Validation:
  The assertion verifies that BigDecimal's compareTo method correctly handles different decimal representations of the same value. This is crucial for accurate price comparisons.

---

Scenario 10: Find Products Returns Immutable List

Details:
  TestName: findProductsReturnsImmutableList
  Description: Verify that the returned list from findByPriceRange is immutable (as toList() returns an unmodifiable list in Java 16+).

Execution:
  Arrange: Create a ProductRepository instance with default sample data.
  Act: Invoke findByPriceRange with minPrice = 50.00 and maxPrice = 500.00, then attempt to add a new element to the returned list.
  Assert: Verify that an UnsupportedOperationException is thrown when attempting to modify the returned list.

Validation:
  The assertion confirms that the toList() method returns an unmodifiable list, protecting the internal state of the repository from external modification. This is important for data integrity.

---

Scenario 11: Find Products With Inverted Price Range Returns Empty List

Details:
  TestName: findProductsWithInvertedPriceRangeReturnsEmptyList
  Description: Verify that when minPrice is greater than maxPrice, no products are returned since no price can satisfy both conditions.

Execution:
  Arrange: Create a ProductRepository instance with default sample data.
  Act: Invoke findByPriceRange with minPrice = 500.00 and maxPrice = 100.00.
  Assert: Verify that the returned list is empty.

Validation:
  The assertion confirms that the method handles logically impossible ranges gracefully. When minPrice > maxPrice, no product can have a price >= minPrice AND <= maxPrice simultaneously.

---

Scenario 12: Find Products After Deleting Product From Range

Details:
  TestName: findProductsAfterDeletingProductFromRange
  Description: Verify that deleted products are no longer returned in price range queries.

Execution:
  Arrange: Create a ProductRepository instance, find the ID of the Mouse product (29.99), and delete it using deleteById.
  Act: Invoke findByPriceRange with minPrice = 20.00 and maxPrice = 50.00.
  Assert: Verify that the returned list is empty (Mouse was the only product in this range).

Validation:
  The assertion confirms that the repository correctly reflects deletions in subsequent queries. This validates the consistency between write and read operations.

---

Scenario 13: Find Products With Large BigDecimal Values

Details:
  TestName: findProductsWithLargeBigDecimalValues
  Description: Verify that the method handles very large BigDecimal values without overflow or precision issues.

Execution:
  Arrange: Create a ProductRepository instance and save a new Product with price = new BigDecimal("999999999.99").
  Act: Invoke findByPriceRange with minPrice = new BigDecimal("999999999.00") and maxPrice = new BigDecimal("1000000000.00").
  Assert: Verify that the returned list contains the newly added high-priced product.

Validation:
  The assertion verifies that BigDecimal handles large monetary values correctly without precision loss. This is important for high-value product catalogs.

*/

// ********RoostGPT********

package com.example.demo.repository;

import com.example.demo.model.Product;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import java.math.BigDecimal;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.stereotype.Repository;
import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicLong;

class ProductRepositoryFindByPriceRangeTest {

	private ProductRepository productRepository;

	@BeforeEach
	void setUp() {
		productRepository = new ProductRepository();
	}

	@Test
	@Tag("valid")
	void findProductsWithinValidPriceRange() {
		// Arrange - ProductRepository is initialized with sample data in constructor
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("50.00"), new BigDecimal("250.00"));
		// Assert
		assertEquals(2, result.size());
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Keyboard")));
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Headphones")));
	}

	@Test
	@Tag("boundary")
	void findProductsWhenPriceEqualsMinimumBoundary() {
		// Arrange - ProductRepository is initialized with sample data in constructor
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("29.99"), new BigDecimal("100.00"));
		// Assert
		assertEquals(2, result.size());
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Mouse")));
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Keyboard")));
	}

	@Test
	@Tag("boundary")
	void findProductsWhenPriceEqualsMaximumBoundary() {
		// Arrange - ProductRepository is initialized with sample data in constructor
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("300.00"), new BigDecimal("399.99"));
		// Assert
		assertEquals(1, result.size());
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Monitor")));
	}

	@Test
	@Tag("boundary")
	void findProductsWhenMinAndMaxPriceAreEqual() {
		// Arrange - ProductRepository is initialized with sample data in constructor
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("199.99"), new BigDecimal("199.99"));
		// Assert
		assertEquals(1, result.size());
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Headphones")));
	}

	@Test
	@Tag("valid")
	void findProductsReturnsEmptyListWhenNoProductsInRange() {
		// Arrange - ProductRepository is initialized with sample data in constructor
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("500.00"), new BigDecimal("600.00"));
		// Assert
		assertNotNull(result);
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("valid")
	void findProductsWithVeryWidePriceRangeReturnsAllProducts() {
		// Arrange - ProductRepository is initialized with sample data in constructor
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("0.00"), new BigDecimal("10000.00"));
		// Assert
		assertEquals(5, result.size());
	}

	@Test
	@Tag("boundary")
	void findProductsWithZeroAsMinimumPrice() {
		// Arrange - ProductRepository is initialized with sample data in constructor
		// Act
		List<Product> result = productRepository.findByPriceRange(BigDecimal.ZERO, new BigDecimal("100.00"));
		// Assert
		assertEquals(2, result.size());
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Mouse")));
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Keyboard")));
	}

	@Test
	@Tag("integration")
	void findProductsAfterAddingNewProductInRange() {
		// Arrange
		Product newProduct = new Product("USB Cable", "USB-C cable", new BigDecimal("15.99"), 100);
		productRepository.save(newProduct);
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("10.00"), new BigDecimal("20.00"));
		// Assert
		assertEquals(1, result.size());
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("USB Cable")));
	}

	@Test
	@Tag("valid")
	void findProductsWithHighPrecisionDecimalPrices() {
		// Arrange - ProductRepository is initialized with sample data in constructor
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("29.990"), new BigDecimal("29.990"));
		// Assert
		assertEquals(1, result.size());
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Mouse")));
	}

	@Test
	@Tag("valid")
	void findProductsReturnsImmutableList() {
		// Arrange - ProductRepository is initialized with sample data in constructor
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("50.00"), new BigDecimal("500.00"));
		// Assert
		assertThrows(UnsupportedOperationException.class, () -> {
			result.add(new Product("Test", "Test", new BigDecimal("100.00"), 10));
		});
	}

	@Test
	@Tag("invalid")
	void findProductsWithInvertedPriceRangeReturnsEmptyList() {
		// Arrange - ProductRepository is initialized with sample data in constructor
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("500.00"), new BigDecimal("100.00"));
		// Assert
		assertNotNull(result);
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("integration")
	void findProductsAfterDeletingProductFromRange() {
		// Arrange
		List<Product> mouseProducts = productRepository.findByPriceRange(new BigDecimal("29.99"),
				new BigDecimal("29.99"));
		assertFalse(mouseProducts.isEmpty());
		Long mouseId = mouseProducts.get(0).getId();
		productRepository.deleteById(mouseId);
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("20.00"), new BigDecimal("50.00"));
		// Assert
		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("valid")
	void findProductsWithLargeBigDecimalValues() {
		// Arrange
		Product expensiveProduct = new Product("Luxury Item", "Very expensive", new BigDecimal("999999999.99"), 1);
		productRepository.save(expensiveProduct);
		// Act
		List<Product> result = productRepository.findByPriceRange(new BigDecimal("999999999.00"),
				new BigDecimal("1000000000.00"));
		// Assert
		assertEquals(1, result.size());
		assertTrue(result.stream().anyMatch(p -> p.getName().equals("Luxury Item")));
	}

}