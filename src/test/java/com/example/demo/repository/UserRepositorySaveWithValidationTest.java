
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-simple-spring-boot-app using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=saveWithValidation_e81fb690d8
ROOST_METHOD_SIG_HASH=saveWithValidation_ec756050bb

Scenario 1: Save New User With Null ID And Allow Update True

Details:
  TestName: saveNewUserWithNullIdAndAllowUpdateTrue
  Description: This test verifies that a new user with a null ID can be successfully saved when allowUpdate is set to true. Since the user has no ID, the existence check should not trigger, and the save operation should proceed normally, assigning a new ID to the user.

Execution:
  Arrange: Create a new User object with null ID, providing valid name, email, and role. Set allowUpdate parameter to true.
  Act: Invoke the saveWithValidation method with the created user and allowUpdate set to true.
  Assert: Verify that the returned user is not null, has a non-null ID assigned, and the user's other properties match the input values.

Validation:
  This assertion verifies that the saveWithValidation method correctly handles new users (with null IDs) and delegates to the save method, which assigns a new ID. The allowUpdate flag should not affect the behavior for new users since there's no existing user to check. This test is significant because it validates the primary use case of creating new users in the system.

---

Scenario 2: Save New User With Null ID And Allow Update False

Details:
  TestName: saveNewUserWithNullIdAndAllowUpdateFalse
  Description: This test verifies that a new user with a null ID can be successfully saved when allowUpdate is set to false. Even though updates are not allowed, this should not affect new user creation since the existence check only applies to users with non-null IDs.

Execution:
  Arrange: Create a new User object with null ID, providing valid name, email, and role. Set allowUpdate parameter to false.
  Act: Invoke the saveWithValidation method with the created user and allowUpdate set to false.
  Assert: Verify that the returned user is not null, has a non-null ID assigned, and the user's properties are correctly saved.

Validation:
  This assertion confirms that the allowUpdate flag only affects existing users (those with non-null IDs that exist in the repository). For new users with null IDs, the method should proceed with saving regardless of the allowUpdate value. This test is important for ensuring that the validation logic doesn't inadvertently block legitimate new user creation.

---

Scenario 3: Update Existing User With Allow Update True

Details:
  TestName: updateExistingUserWithAllowUpdateTrue
  Description: This test verifies that an existing user can be successfully updated when allowUpdate is set to true. The method should recognize that the user exists but allow the update operation to proceed because the allowUpdate flag is true.

Execution:
  Arrange: First, save a user to the repository to ensure it exists. Then, create a modified version of that user with the same ID but different properties (e.g., different name or email). Set allowUpdate parameter to true.
  Act: Invoke the saveWithValidation method with the modified user and allowUpdate set to true.
  Assert: Verify that the method returns successfully without throwing an exception, and the returned user contains the updated properties.

Validation:
  This assertion verifies that the saveWithValidation method correctly allows updates to existing users when the allowUpdate flag is true. The existence check should pass, but the IllegalStateException should not be thrown. This test is crucial for validating the update functionality, which is a common operation in user management systems.

---

Scenario 4: Update Existing User With Allow Update False Throws Exception

Details:
  TestName: updateExistingUserWithAllowUpdateFalseThrowsException
  Description: This test verifies that attempting to update an existing user when allowUpdate is set to false results in an IllegalStateException. This is the core validation logic of the method, preventing unintended overwrites of existing user data.

Execution:
  Arrange: First, save a user to the repository to ensure it exists. Then, create a user object with the same ID as the existing user. Set allowUpdate parameter to false.
  Act: Invoke the saveWithValidation method with the user having an existing ID and allowUpdate set to false.
  Assert: Verify that an IllegalStateException is thrown with a message containing "User already exists with id:" followed by the user's ID.

Validation:
  This assertion confirms that the method correctly enforces the allowUpdate constraint. When a user with an existing ID is passed and allowUpdate is false, the method should prevent the update by throwing an IllegalStateException. This test is critical for ensuring data integrity and preventing accidental overwrites of existing user records in scenarios where updates should be explicitly controlled.

---

Scenario 5: Save User With Non Existing ID And Allow Update False

Details:
  TestName: saveUserWithNonExistingIdAndAllowUpdateFalse
  Description: This test verifies that a user with a non-null ID that doesn't exist in the repository can be saved even when allowUpdate is false. The existence check should return false, allowing the save operation to proceed.

Execution:
  Arrange: Create a new User object and manually set an ID that doesn't exist in the repository (e.g., 9999L). Ensure this ID is not present by checking the repository state. Set allowUpdate parameter to false.
  Act: Invoke the saveWithValidation method with the user having a non-existing ID and allowUpdate set to false.
  Assert: Verify that the method returns successfully without throwing an exception, and the user is saved with the specified ID.

Validation:
  This assertion verifies that the method's validation logic correctly distinguishes between existing and non-existing users. Even with allowUpdate set to false, if the user ID doesn't exist in the repository, the save operation should proceed. This test is important for scenarios where users might be created with pre-assigned IDs from external systems or during data migration.

---

Scenario 6: Save User With Non Existing ID And Allow Update True

Details:
  TestName: saveUserWithNonExistingIdAndAllowUpdateTrue
  Description: This test verifies that a user with a non-null ID that doesn't exist in the repository can be saved when allowUpdate is true. This should work identically to the scenario with allowUpdate false, since the user doesn't exist.

Execution:
  Arrange: Create a new User object and manually set an ID that doesn't exist in the repository (e.g., 8888L). Set allowUpdate parameter to true.
  Act: Invoke the saveWithValidation method with the user having a non-existing ID and allowUpdate set to true.
  Assert: Verify that the method returns successfully, and the user is saved with the specified ID.

Validation:
  This assertion confirms that when a user with a non-existing ID is provided, the allowUpdate flag doesn't affect the outcome since there's no existing user to update. The method should successfully save the user regardless of the allowUpdate value. This test ensures consistent behavior across different allowUpdate values for non-existing users.

---

Scenario 7: Save Null User With Allow Update True

Details:
  TestName: saveNullUserWithAllowUpdateTrue
  Description: This test verifies the behavior when a null user object is passed to the saveWithValidation method with allowUpdate set to true. This tests the method's handling of invalid input.

Execution:
  Arrange: Set the user parameter to null. Set allowUpdate parameter to true.
  Act: Invoke the saveWithValidation method with null user and allowUpdate set to true.
  Assert: Verify that the method either throws a NullPointerException or handles the null case appropriately based on the implementation's error handling strategy.

Validation:
  This assertion verifies the method's behavior with null input, which is an important edge case for defensive programming. The test helps ensure that the method fails gracefully or throws an appropriate exception when given invalid input. This is significant for preventing unexpected runtime errors and ensuring robust error handling.

---

Scenario 8: Save Null User With Allow Update False

Details:
  TestName: saveNullUserWithAllowUpdateFalse
  Description: This test verifies the behavior when a null user object is passed to the saveWithValidation method with allowUpdate set to false. This ensures consistent null handling regardless of the allowUpdate flag.

Execution:
  Arrange: Set the user parameter to null. Set allowUpdate parameter to false.
  Act: Invoke the saveWithValidation method with null user and allowUpdate set to false.
  Assert: Verify that the method either throws a NullPointerException or handles the null case appropriately, with behavior consistent with the allowUpdate true scenario.

Validation:
  This assertion ensures that null handling is consistent regardless of the allowUpdate flag value. The method should handle null inputs in the same way whether updates are allowed or not. This test is important for verifying that the validation logic doesn't introduce inconsistent behavior based on the allowUpdate parameter when dealing with invalid inputs.

---

Scenario 9: Save Existing User Multiple Times With Allow Update True

Details:
  TestName: saveExistingUserMultipleTimesWithAllowUpdateTrue
  Description: This test verifies that an existing user can be updated multiple times consecutively when allowUpdate is true. This tests the method's behavior in scenarios involving repeated updates.

Execution:
  Arrange: Save an initial user to the repository. Then create multiple modified versions of the user with the same ID but different properties. Set allowUpdate parameter to true.
  Act: Invoke the saveWithValidation method multiple times with different versions of the user, all with allowUpdate set to true.
  Assert: Verify that each invocation succeeds without throwing an exception, and the final state of the user in the repository reflects the last update.

Validation:
  This assertion verifies that the method supports repeated updates to the same user when allowUpdate is true. This is important for scenarios where user data needs to be updated multiple times in succession, such as during batch processing or iterative data modifications. The test ensures that the method doesn't have any state-related issues that would prevent consecutive updates.

---

Scenario 10: Exception Message Contains Correct User ID

Details:
  TestName: exceptionMessageContainsCorrectUserId
  Description: This test verifies that when an IllegalStateException is thrown for attempting to update an existing user with allowUpdate false, the exception message contains the correct user ID. This ensures proper error reporting for debugging and logging purposes.

Execution:
  Arrange: Save a user with a specific ID to the repository. Create another user object with the same ID. Set allowUpdate parameter to false.
  Act: Invoke the saveWithValidation method with the existing user ID and allowUpdate set to false, catching the thrown IllegalStateException.
  Assert: Verify that the exception message contains the text "User already exists with id:" followed by the specific user ID that was used.

Validation:
  This assertion ensures that the error message provides meaningful information about which user caused the conflict. Proper error messages are crucial for debugging, logging, and providing useful feedback to calling code. This test verifies that the exception not only is thrown at the right time but also contains the correct contextual information.

---

Scenario 11: Save User After Deletion With Allow Update False

Details:
  TestName: saveUserAfterDeletionWithAllowUpdateFalse
  Description: This test verifies that a user can be saved with the same ID after the original user with that ID has been deleted, even when allowUpdate is false. The existence check should return false after deletion, allowing the save to proceed.

Execution:
  Arrange: Save a user to the repository, note its ID, then delete that user using deleteById. Create a new user object with the same ID as the deleted user. Set allowUpdate parameter to false.
  Act: Invoke the saveWithValidation method with the new user having the previously deleted user's ID and allowUpdate set to false.
  Assert: Verify that the method succeeds without throwing an exception, and the new user is saved successfully.

Validation:
  This assertion verifies that the existence check correctly reflects the current state of the repository. After a user is deleted, their ID should be available for reuse. This test is important for ensuring that the method doesn't have any caching or state issues that would incorrectly report a deleted user as still existing. It validates the proper integration with the existsById method.

---

Scenario 12: Verify Save Method Is Called For New User

Details:
  TestName: verifySaveMethodIsCalledForNewUser
  Description: This test verifies that the internal save method is invoked when saving a new user through saveWithValidation. This ensures that the method correctly delegates the actual save operation.

Execution:
  Arrange: Create a new User object with null ID. Set allowUpdate parameter to either true or false (doesn't matter for new users).
  Act: Invoke the saveWithValidation method with the new user.
  Assert: Verify that the user is successfully saved by checking that it can be retrieved from the repository using findById with the assigned ID, confirming that the save method was called.

Validation:
  This assertion verifies that saveWithValidation correctly delegates to the save method for new users. The test confirms that the method doesn't just perform validation but also completes the save operation. This is important for ensuring that the method fulfills its complete contract of both validating and saving users.

---

Scenario 13: Verify Save Method Is Called For Existing User With Allow Update True

Details:
  TestName: verifySaveMethodIsCalledForExistingUserWithAllowUpdateTrue
  Description: This test verifies that the internal save method is invoked when updating an existing user with allowUpdate set to true. This ensures proper delegation of the update operation.

Execution:
  Arrange: Save an initial user to the repository. Create a modified version of the user with the same ID but different properties. Set allowUpdate parameter to true.
  Act: Invoke the saveWithValidation method with the modified user and allowUpdate true.
  Assert: Verify that the user's properties are updated in the repository by retrieving the user with findById and checking that the properties match the updated values, confirming that save was called.

Validation:
  This assertion confirms that when updates are allowed, the method properly delegates to the save method to persist the changes. The test ensures that the validation logic doesn't interfere with the actual update operation when updates are permitted. This is crucial for verifying the complete update workflow.

---

Scenario 14: Verify ExistsById Is Called During Validation

Details:
  TestName: verifyExistsByIdIsCalledDuringValidation
  Description: This test verifies that the existsById method is invoked during the validation process when a user with a non-null ID is being saved. This ensures that the existence check is actually performed.

Execution:
  Arrange: Create a user with a specific non-null ID that exists in the repository. Set allowUpdate parameter to false.
  Act: Invoke the saveWithValidation method with the user, expecting an IllegalStateException.
  Assert: Verify that the exception is thrown, which indirectly confirms that existsById was called and returned true, triggering the validation logic.

Validation:
  This assertion verifies that the method performs the necessary existence check before attempting to save. The fact that the exception is thrown confirms that existsById was called and returned true. This test is important for ensuring that the validation logic is actually executed and not bypassed.

---

Scenario 15: Save User With ID Zero And Allow Update False

Details:
  TestName: saveUserWithIdZeroAndAllowUpdateFalse
  Description: This test verifies the behavior when saving a user with ID set to 0 (zero) and allowUpdate is false. This tests the edge case of using zero as an ID value, which is a valid Long but might be considered a special case.

Execution:
  Arrange: Create a new User object and manually set its ID to 0L. Ensure no user with ID 0 exists in the repository. Set allowUpdate parameter to false.
  Act: Invoke the saveWithValidation method with the user having ID 0 and allowUpdate set to false.
  Assert: Verify that the method succeeds without throwing an exception, and the user is saved with ID 0.

Validation:
  This assertion verifies that the method correctly handles the edge case of ID 0. Since 0 is a valid Long value and the user doesn't exist, the save should proceed. This test is important for ensuring that the method doesn't treat 0 as equivalent to null or as a special case that would trigger different behavior.

---

Scenario 16: Concurrent Save Operations With Same User ID And Allow Update False

Details:
  TestName: concurrentSaveOperationsWithSameUserIdAndAllowUpdateFalse
  Description: This test verifies the behavior when multiple threads attempt to save users with the same ID concurrently with allowUpdate set to false. This tests thread safety and race condition handling.

Execution:
  Arrange: Create multiple user objects with the same non-existing ID. Set allowUpdate parameter to false for all operations. Prepare multiple threads to invoke saveWithValidation concurrently.
  Act: Execute the saveWithValidation method from multiple threads simultaneously with users having the same ID.
  Assert: Verify that at least one thread succeeds in saving the user, and subsequent threads either succeed (if they execute before the first save completes) or throw IllegalStateException (if they execute after the first save completes).

Validation:
  This assertion verifies the thread safety of the saveWithValidation method when used with the ConcurrentHashMap-based storage. The test ensures that the method behaves correctly under concurrent access, which is important for multi-threaded applications. The exact behavior depends on the timing of the existsById check and the save operation, but the method should handle concurrent access gracefully without data corruption.

---

Scenario 17: Save User With Very Large ID Value

Details:
  TestName: saveUserWithVeryLargeIdValue
  Description: This test verifies that the method can handle users with very large ID values (near Long.MAX_VALUE). This tests the method's behavior with extreme but valid ID values.

Execution:
  Arrange: Create a new User object and set its ID to a very large value (e.g., Long.MAX_VALUE - 1). Ensure this ID doesn't exist in the repository. Set allowUpdate parameter to false.
  Act: Invoke the saveWithValidation method with the user having a very large ID.
  Assert: Verify that the method succeeds without throwing an exception, and the user is saved with the specified large ID.

Validation:
  This assertion verifies that the method correctly handles extreme but valid ID values. There should be no special handling or overflow issues with large Long values. This test is important for ensuring that the method works correctly across the entire range of valid Long values, which is crucial for systems that might use large ID values from external sources.

---

Scenario 18: Save User With Negative ID Value

Details:
  TestName: saveUserWithNegativeIdValue
  Description: This test verifies the behavior when saving a user with a negative ID value. While negative IDs might be unusual, they are valid Long values and the method should handle them consistently.

Execution:
  Arrange: Create a new User object and set its ID to a negative value (e.g., -1L). Ensure this ID doesn't exist in the repository. Set allowUpdate parameter to false.
  Act: Invoke the saveWithValidation method with the user having a negative ID.
  Assert: Verify that the method succeeds without throwing an exception, and the user is saved with the specified negative ID.

Validation:
  This assertion verifies that the method doesn't have any implicit assumptions about IDs being positive. Negative IDs are valid Long values and should be handled the same way as positive IDs. This test is important for ensuring consistent behavior across all valid ID values and for systems that might use negative IDs for special purposes.

---

Scenario 19: Return Value Contains Same User Reference For New User

Details:
  TestName: returnValueContainsSameUserReferenceForNewUser
  Description: This test verifies that the returned user object from saveWithValidation is the same object that was passed in (or at least contains the same data), ensuring that the method returns the saved user correctly for new users.

Execution:
  Arrange: Create a new User object with null ID and specific properties. Set allowUpdate parameter to true.
  Act: Invoke the saveWithValidation method with the new user and capture the returned user object.
  Assert: Verify that the returned user has the same properties as the input user (name, email, role) and has been assigned a non-null ID.

Validation:
  This assertion verifies that the method correctly returns the saved user object with all its properties intact and with an assigned ID. This is important for callers who need to access the saved user's ID or verify that the save operation completed successfully. The test ensures that the method's return value is meaningful and useful.

---

Scenario 20: Return Value Contains Updated User For Existing User With Allow Update True

Details:
  TestName: returnValueContainsUpdatedUserForExistingUserWithAllowUpdateTrue
  Description: This test verifies that when updating an existing user with allowUpdate true, the returned user object contains the updated properties.

Execution:
  Arrange: Save an initial user to the repository. Create a modified version of the user with the same ID but different properties. Set allowUpdate parameter to true.
  Act: Invoke the saveWithValidation method with the modified user and allowUpdate true, capturing the returned user object.
  Assert: Verify that the returned user contains the updated properties and the same ID as the original user.

Validation:
  This assertion confirms that the method returns the updated user object after a successful update operation. This is important for callers who need to work with the updated user data immediately after the save operation. The test ensures that the return value accurately reflects the state of the user after the update.

*/

// ********RoostGPT********

package com.example.demo.repository;

import com.example.demo.model.User;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import java.util.Optional;
import java.util.concurrent.CountDownLatch;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;
import java.util.concurrent.atomic.AtomicInteger;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.stereotype.Repository;
import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicLong;

class UserRepositorySaveWithValidationTest {

	private UserRepository userRepository;

	@BeforeEach
	void setUp() {
		userRepository = new UserRepository();
	}

	@Test
	@Tag("valid")
	void saveNewUserWithNullIdAndAllowUpdateTrue() {
		User newUser = new User("John Doe", "john@example.com", "USER");

		User savedUser = userRepository.saveWithValidation(newUser, true);

		assertNotNull(savedUser);
		assertNotNull(savedUser.getId());
		assertEquals("John Doe", savedUser.getName());
		assertEquals("john@example.com", savedUser.getEmail());
		assertEquals("USER", savedUser.getRole());
	}

	@Test
	@Tag("valid")
	void saveNewUserWithNullIdAndAllowUpdateFalse() {
		User newUser = new User("Jane Doe", "jane@example.com", "ADMIN");

		User savedUser = userRepository.saveWithValidation(newUser, false);

		assertNotNull(savedUser);
		assertNotNull(savedUser.getId());
		assertEquals("Jane Doe", savedUser.getName());
		assertEquals("jane@example.com", savedUser.getEmail());
		assertEquals("ADMIN", savedUser.getRole());
	}

	@Test
	@Tag("valid")
	void updateExistingUserWithAllowUpdateTrue() {
		User initialUser = new User("Initial Name", "initial@example.com", "USER");
		User savedUser = userRepository.save(initialUser);
		Long userId = savedUser.getId();

		User updatedUser = new User("Updated Name", "updated@example.com", "ADMIN");
		updatedUser.setId(userId);

		User result = userRepository.saveWithValidation(updatedUser, true);

		assertNotNull(result);
		assertEquals(userId, result.getId());
		assertEquals("Updated Name", result.getName());
		assertEquals("updated@example.com", result.getEmail());
		assertEquals("ADMIN", result.getRole());
	}

	@Test
	@Tag("invalid")
	void updateExistingUserWithAllowUpdateFalseThrowsException() {
		User initialUser = new User("Existing User", "existing@example.com", "USER");
		User savedUser = userRepository.save(initialUser);
		Long userId = savedUser.getId();

		User duplicateUser = new User("Duplicate User", "duplicate@example.com", "USER");
		duplicateUser.setId(userId);

		IllegalStateException exception = assertThrows(IllegalStateException.class, () -> {
			userRepository.saveWithValidation(duplicateUser, false);
		});

		assertTrue(exception.getMessage().contains("User already exists with id: " + userId));
	}

	@Test
	@Tag("valid")
	void saveUserWithNonExistingIdAndAllowUpdateFalse() {
		User newUser = new User("New User", "newuser@example.com", "USER");
		newUser.setId(9999L);

		assertFalse(userRepository.existsById(9999L));

		User savedUser = userRepository.saveWithValidation(newUser, false);

		assertNotNull(savedUser);
		assertEquals(9999L, savedUser.getId());
		assertEquals("New User", savedUser.getName());
		assertTrue(userRepository.existsById(9999L));
	}

	@Test
	@Tag("valid")
	void saveUserWithNonExistingIdAndAllowUpdateTrue() {
		User newUser = new User("Another User", "another@example.com", "ADMIN");
		newUser.setId(8888L);

		assertFalse(userRepository.existsById(8888L));

		User savedUser = userRepository.saveWithValidation(newUser, true);

		assertNotNull(savedUser);
		assertEquals(8888L, savedUser.getId());
		assertEquals("Another User", savedUser.getName());
		assertTrue(userRepository.existsById(8888L));
	}

	@Test
	@Tag("invalid")
	void saveNullUserWithAllowUpdateTrue() {
		assertThrows(NullPointerException.class, () -> {
			userRepository.saveWithValidation(null, true);
		});
	}

	@Test
	@Tag("invalid")
	void saveNullUserWithAllowUpdateFalse() {
		assertThrows(NullPointerException.class, () -> {
			userRepository.saveWithValidation(null, false);
		});
	}

	@Test
	@Tag("valid")
	void saveExistingUserMultipleTimesWithAllowUpdateTrue() {
		User initialUser = new User("Multi Update User", "multi@example.com", "USER");
		User savedUser = userRepository.save(initialUser);
		Long userId = savedUser.getId();

		User firstUpdate = new User("First Update", "first@example.com", "USER");
		firstUpdate.setId(userId);
		userRepository.saveWithValidation(firstUpdate, true);

		User secondUpdate = new User("Second Update", "second@example.com", "ADMIN");
		secondUpdate.setId(userId);
		userRepository.saveWithValidation(secondUpdate, true);

		User thirdUpdate = new User("Third Update", "third@example.com", "USER");
		thirdUpdate.setId(userId);
		User finalResult = userRepository.saveWithValidation(thirdUpdate, true);

		assertEquals("Third Update", finalResult.getName());
		assertEquals("third@example.com", finalResult.getEmail());
		assertEquals("USER", finalResult.getRole());

		Optional<User> retrievedUser = userRepository.findById(userId);
		assertTrue(retrievedUser.isPresent());
		assertEquals("Third Update", retrievedUser.get().getName());
	}

	@Test
	@Tag("invalid")
	void exceptionMessageContainsCorrectUserId() {
		User user = new User("Test User", "test@example.com", "USER");
		User savedUser = userRepository.save(user);
		Long specificId = savedUser.getId();

		User duplicateUser = new User("Duplicate", "duplicate@example.com", "USER");
		duplicateUser.setId(specificId);

		IllegalStateException exception = assertThrows(IllegalStateException.class, () -> {
			userRepository.saveWithValidation(duplicateUser, false);
		});

		String expectedMessage = "User already exists with id: " + specificId;
		assertEquals(expectedMessage, exception.getMessage());
	}

	@Test
	@Tag("valid")
	void saveUserAfterDeletionWithAllowUpdateFalse() {
		User user = new User("To Be Deleted", "delete@example.com", "USER");
		User savedUser = userRepository.save(user);
		Long userId = savedUser.getId();

		assertTrue(userRepository.deleteById(userId));
		assertFalse(userRepository.existsById(userId));

		User newUser = new User("Reused ID User", "reused@example.com", "ADMIN");
		newUser.setId(userId);

		User result = userRepository.saveWithValidation(newUser, false);

		assertNotNull(result);
		assertEquals(userId, result.getId());
		assertEquals("Reused ID User", result.getName());
		assertTrue(userRepository.existsById(userId));
	}

	@Test
	@Tag("integration")
	void verifySaveMethodIsCalledForNewUser() {
		User newUser = new User("Verify Save", "verifysave@example.com", "USER");

		User savedUser = userRepository.saveWithValidation(newUser, true);

		assertNotNull(savedUser.getId());
		Optional<User> retrievedUser = userRepository.findById(savedUser.getId());
		assertTrue(retrievedUser.isPresent());
		assertEquals("Verify Save", retrievedUser.get().getName());
		assertEquals("verifysave@example.com", retrievedUser.get().getEmail());
	}

	@Test
	@Tag("integration")
	void verifySaveMethodIsCalledForExistingUserWithAllowUpdateTrue() {
		User initialUser = new User("Original", "original@example.com", "USER");
		User savedUser = userRepository.save(initialUser);
		Long userId = savedUser.getId();

		User modifiedUser = new User("Modified", "modified@example.com", "ADMIN");
		modifiedUser.setId(userId);

		userRepository.saveWithValidation(modifiedUser, true);

		Optional<User> retrievedUser = userRepository.findById(userId);
		assertTrue(retrievedUser.isPresent());
		assertEquals("Modified", retrievedUser.get().getName());
		assertEquals("modified@example.com", retrievedUser.get().getEmail());
		assertEquals("ADMIN", retrievedUser.get().getRole());
	}

	@Test
	@Tag("integration")
	void verifyExistsByIdIsCalledDuringValidation() {
		User user = new User("Exists Check", "existscheck@example.com", "USER");
		User savedUser = userRepository.save(user);
		Long userId = savedUser.getId();

		assertTrue(userRepository.existsById(userId));

		User duplicateUser = new User("Duplicate", "duplicate@example.com", "USER");
		duplicateUser.setId(userId);

		IllegalStateException exception = assertThrows(IllegalStateException.class, () -> {
			userRepository.saveWithValidation(duplicateUser, false);
		});

		assertNotNull(exception);
		assertTrue(exception.getMessage().contains("User already exists with id:"));
	}

	@Test
	@Tag("boundary")
	void saveUserWithIdZeroAndAllowUpdateFalse() {
		User userWithZeroId = new User("Zero ID User", "zeroid@example.com", "USER");
		userWithZeroId.setId(0L);

		assertFalse(userRepository.existsById(0L));

		User savedUser = userRepository.saveWithValidation(userWithZeroId, false);

		assertNotNull(savedUser);
		assertEquals(0L, savedUser.getId());
		assertEquals("Zero ID User", savedUser.getName());
		assertTrue(userRepository.existsById(0L));
	}

	@Test
	@Tag("integration")
	void concurrentSaveOperationsWithSameUserIdAndAllowUpdateFalse() throws InterruptedException {
		Long targetId = 7777L;
		int threadCount = 5;
		ExecutorService executorService = Executors.newFixedThreadPool(threadCount);
		CountDownLatch latch = new CountDownLatch(threadCount);
		AtomicInteger successCount = new AtomicInteger(0);
		AtomicInteger exceptionCount = new AtomicInteger(0);

		for (int i = 0; i < threadCount; i++) {
			final int index = i;
			executorService.submit(() -> {
				try {
					User user = new User("Concurrent User " + index, "concurrent" + index + "@example.com", "USER");
					user.setId(targetId);
					userRepository.saveWithValidation(user, false);
					successCount.incrementAndGet();
				}
				catch (IllegalStateException e) {
					exceptionCount.incrementAndGet();
				}
				finally {
					latch.countDown();
				}
			});
		}

		latch.await();
		executorService.shutdown();

		assertTrue(successCount.get() >= 1);
		assertTrue(successCount.get() + exceptionCount.get() == threadCount);
		assertTrue(userRepository.existsById(targetId));
	}

	@Test
	@Tag("boundary")
	void saveUserWithVeryLargeIdValue() {
		Long largeId = Long.MAX_VALUE - 1;
		User userWithLargeId = new User("Large ID User", "largeid@example.com", "USER");
		userWithLargeId.setId(largeId);

		assertFalse(userRepository.existsById(largeId));

		User savedUser = userRepository.saveWithValidation(userWithLargeId, false);

		assertNotNull(savedUser);
		assertEquals(largeId, savedUser.getId());
		assertEquals("Large ID User", savedUser.getName());
		assertTrue(userRepository.existsById(largeId));
	}

	@Test
	@Tag("boundary")
	void saveUserWithNegativeIdValue() {
		Long negativeId = -1L;
		User userWithNegativeId = new User("Negative ID User", "negativeid@example.com", "ADMIN");
		userWithNegativeId.setId(negativeId);

		assertFalse(userRepository.existsById(negativeId));

		User savedUser = userRepository.saveWithValidation(userWithNegativeId, false);

		assertNotNull(savedUser);
		assertEquals(negativeId, savedUser.getId());
		assertEquals("Negative ID User", savedUser.getName());
		assertTrue(userRepository.existsById(negativeId));
	}

	@Test
	@Tag("valid")
	void returnValueContainsSameUserReferenceForNewUser() {
		User newUser = new User("Return Value Test", "returnvalue@example.com", "USER");

		User returnedUser = userRepository.saveWithValidation(newUser, true);

		assertNotNull(returnedUser);
		assertNotNull(returnedUser.getId());
		assertEquals("Return Value Test", returnedUser.getName());
		assertEquals("returnvalue@example.com", returnedUser.getEmail());
		assertEquals("USER", returnedUser.getRole());
	}

	@Test
	@Tag("valid")
	void returnValueContainsUpdatedUserForExistingUserWithAllowUpdateTrue() {
		User initialUser = new User("Initial", "initial@example.com", "USER");
		User savedUser = userRepository.save(initialUser);
		Long userId = savedUser.getId();

		User modifiedUser = new User("Updated", "updated@example.com", "ADMIN");
		modifiedUser.setId(userId);

		User returnedUser = userRepository.saveWithValidation(modifiedUser, true);

		assertNotNull(returnedUser);
		assertEquals(userId, returnedUser.getId());
		assertEquals("Updated", returnedUser.getName());
		assertEquals("updated@example.com", returnedUser.getEmail());
		assertEquals("ADMIN", returnedUser.getRole());
	}

}