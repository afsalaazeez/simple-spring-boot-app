
// ********RoostGPT********
/*
Test generated by RoostGPT for test Dec-8-Java using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=findAll_e3226f7b62
ROOST_METHOD_SIG_HASH=findAll_a2bf6614ea

Scenario 1: Retrieve All Products From Repository With Initial Sample Data

Details:
  TestName: findAllReturnsInitialSampleProducts
  Description: Verify that the findAll method returns all products that were initialized in the repository constructor. The repository is pre-populated with 5 sample products (Laptop, Mouse, Keyboard, Monitor, Headphones) upon instantiation.

Execution:
  Arrange: Create a new instance of ProductRepository, which automatically initializes with 5 sample products.
  Act: Invoke the findAll() method on the repository instance.
  Assert: Verify that the returned list contains exactly 5 products and is not null.

Validation:
  The assertion confirms that the repository correctly returns all pre-populated products. This test is significant because it validates the basic retrieval functionality and ensures the constructor properly initializes the sample data, which is essential for the application's demo functionality.

---

Scenario 2: Verify findAll Returns A New ArrayList Instance

Details:
  TestName: findAllReturnsNewArrayListNotBackingMap
  Description: Verify that the findAll method returns a new ArrayList instance that is independent of the internal ConcurrentHashMap. Modifications to the returned list should not affect the repository's internal state.

Execution:
  Arrange: Create a new instance of ProductRepository.
  Act: Invoke findAll() and then clear the returned list.
  Assert: Call findAll() again and verify that the original products are still present in the repository (list size should still be 5).

Validation:
  This assertion verifies that the method creates a defensive copy of the products collection. This is crucial for maintaining data integrity and preventing external code from accidentally modifying the repository's internal state through the returned list.

---

Scenario 3: Retrieve All Products After Adding A New Product

Details:
  TestName: findAllIncludesNewlySavedProduct
  Description: Verify that the findAll method returns all products including any newly added product after the initial sample data.

Execution:
  Arrange: Create a new ProductRepository instance and save a new Product with name "Tablet", description "10-inch tablet", price 499.99, and stock 10.
  Act: Invoke the findAll() method.
  Assert: Verify that the returned list contains 6 products (5 initial + 1 new).

Validation:
  This test confirms that the findAll method dynamically reflects changes to the repository. It validates that newly saved products are properly included in subsequent findAll calls, which is essential for the repository's CRUD operations to work correctly.

---

Scenario 4: Retrieve All Products After Deleting A Product

Details:
  TestName: findAllExcludesDeletedProduct
  Description: Verify that the findAll method does not return products that have been deleted from the repository.

Execution:
  Arrange: Create a new ProductRepository instance and delete a product by its ID (e.g., ID 1).
  Act: Invoke the findAll() method.
  Assert: Verify that the returned list contains 4 products and none of them has ID 1.

Validation:
  This test ensures that the findAll method accurately reflects the current state of the repository after deletion operations. It validates the consistency between delete operations and retrieval operations, which is critical for maintaining data accuracy.

---

Scenario 5: Verify findAll Returns Empty List When All Products Are Deleted

Details:
  TestName: findAllReturnsEmptyListWhenRepositoryIsEmpty
  Description: Verify that the findAll method returns an empty list when all products have been removed from the repository.

Execution:
  Arrange: Create a new ProductRepository instance and delete all products by iterating through IDs 1 to 5 using deleteById method.
  Act: Invoke the findAll() method.
  Assert: Verify that the returned list is empty (size equals 0) and not null.

Validation:
  This test validates the edge case where the repository contains no products. It ensures that the method handles an empty state gracefully by returning an empty list rather than null, which prevents NullPointerException in client code.

---

Scenario 6: Verify findAll Returns Products With Correct Data Integrity

Details:
  TestName: findAllReturnsProductsWithCorrectAttributes
  Description: Verify that the products returned by findAll contain the correct attribute values as they were saved, ensuring data integrity is maintained.

Execution:
  Arrange: Create a new ProductRepository instance.
  Act: Invoke findAll() and retrieve the list of products.
  Assert: Verify that at least one product in the list has the expected name "Laptop" and that products have non-null IDs assigned.

Validation:
  This test confirms that the findAll method returns products with their complete and accurate data. It validates that the repository maintains data integrity during storage and retrieval, which is fundamental for the application's business logic.

---

Scenario 7: Verify findAll Is Thread-Safe With Concurrent Access

Details:
  TestName: findAllHandlesConcurrentAccess
  Description: Verify that the findAll method can be safely called concurrently without causing data corruption or exceptions, given that the underlying storage uses ConcurrentHashMap.

Execution:
  Arrange: Create a new ProductRepository instance.
  Act: Invoke findAll() multiple times in rapid succession (simulating concurrent access).
  Assert: Verify that each call returns a valid list with consistent size (5 products) and no exceptions are thrown.

Validation:
  This test validates the thread-safety of the findAll method. Since the repository uses ConcurrentHashMap, concurrent read operations should be safe. This is important for applications where multiple threads may access the repository simultaneously.

---

Scenario 8: Verify findAll After Multiple Save Operations On Same Product

Details:
  TestName: findAllReflectsUpdatedProductAfterMultipleSaves
  Description: Verify that when a product is updated (saved multiple times with the same ID), findAll returns the latest version of the product without duplicates.

Execution:
  Arrange: Create a new ProductRepository instance, retrieve a product by ID 1, modify its name, and save it again.
  Act: Invoke the findAll() method.
  Assert: Verify that the list still contains exactly 5 products and the product with ID 1 has the updated name.

Validation:
  This test ensures that the repository correctly handles update operations and that findAll returns the most recent state of each product. It validates that the save method properly updates existing entries rather than creating duplicates.

*/

// ********RoostGPT********

package com.example.demo.repository;

import com.example.demo.model.Product;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import java.math.BigDecimal;
import java.util.List;
import java.util.concurrent.CountDownLatch;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;
import java.util.concurrent.atomic.AtomicBoolean;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import org.springframework.stereotype.Repository;
import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicLong;

class ProductRepositoryFindAllTest {

	private ProductRepository productRepository;

	@BeforeEach
	void setUp() {
		productRepository = new ProductRepository();
	}

	@Test
	@Tag("valid")
	void findAllReturnsInitialSampleProducts() {
		// Arrange - repository is already initialized with 5 sample products in
		// constructor
		// Act
		List<Product> products = productRepository.findAll();
		// Assert
		assertNotNull(products);
		assertEquals(5, products.size());
	}

	@Test
	@Tag("valid")
	void findAllReturnsNewArrayListNotBackingMap() {
		// Arrange
		List<Product> firstList = productRepository.findAll();
		// Act
		firstList.clear();
		// Assert
		List<Product> secondList = productRepository.findAll();
		assertNotNull(secondList);
		assertEquals(5, secondList.size());
	}

	@Test
	@Tag("valid")
	void findAllIncludesNewlySavedProduct() {
		// Arrange
		Product newProduct = new Product("Tablet", "10-inch tablet", new BigDecimal("499.99"), 10);
		productRepository.save(newProduct);
		// Act
		List<Product> products = productRepository.findAll();
		// Assert
		assertNotNull(products);
		assertEquals(6, products.size());
		assertTrue(products.stream().anyMatch(p -> "Tablet".equals(p.getName())));
	}

	@Test
	@Tag("valid")
	void findAllExcludesDeletedProduct() {
		// Arrange
		productRepository.deleteById(1L);
		// Act
		List<Product> products = productRepository.findAll();
		// Assert
		assertNotNull(products);
		assertEquals(4, products.size());
		assertTrue(products.stream().noneMatch(p -> p.getId() != null && p.getId().equals(1L)));
	}

	@Test
	@Tag("boundary")
	void findAllReturnsEmptyListWhenRepositoryIsEmpty() {
		// Arrange
		for (long i = 1; i <= 5; i++) {
			productRepository.deleteById(i);
		}
		// Act
		List<Product> products = productRepository.findAll();
		// Assert
		assertNotNull(products);
		assertTrue(products.isEmpty());
		assertEquals(0, products.size());
	}

	@Test
	@Tag("valid")
	void findAllReturnsProductsWithCorrectAttributes() {
		// Arrange - repository is already initialized
		// Act
		List<Product> products = productRepository.findAll();
		// Assert
		assertNotNull(products);
		assertTrue(products.stream().anyMatch(p -> "Laptop".equals(p.getName())));
		assertTrue(products.stream().allMatch(p -> p.getId() != null));

		Product laptop = products.stream().filter(p -> "Laptop".equals(p.getName())).findFirst().orElse(null);

		assertNotNull(laptop);
		assertEquals("High-performance laptop", laptop.getDescription());
		assertEquals(new BigDecimal("999.99"), laptop.getPrice());
		assertEquals(15, laptop.getStock());
	}

	@Test
	@Tag("integration")
	void findAllHandlesConcurrentAccess() throws InterruptedException {
		// Arrange
		int numberOfThreads = 10;
		ExecutorService executorService = Executors.newFixedThreadPool(numberOfThreads);
		CountDownLatch latch = new CountDownLatch(numberOfThreads);
		AtomicBoolean exceptionOccurred = new AtomicBoolean(false);
		AtomicBoolean inconsistentData = new AtomicBoolean(false);
		// Act
		for (int i = 0; i < numberOfThreads; i++) {
			executorService.submit(() -> {
				try {
					List<Product> products = productRepository.findAll();
					if (products == null || products.size() != 5) {
						inconsistentData.set(true);
					}
				}
				catch (Exception e) {
					exceptionOccurred.set(true);
				}
				finally {
					latch.countDown();
				}
			});
		}
		latch.await();
		executorService.shutdown();
		// Assert
		assertFalse(exceptionOccurred.get());
		assertFalse(inconsistentData.get());
	}

	@Test
	@Tag("valid")
	void findAllReflectsUpdatedProductAfterMultipleSaves() {
		// Arrange
		Product existingProduct = productRepository.findById(1L).orElse(null);
		assertNotNull(existingProduct);

		existingProduct.setName("Updated Laptop");
		productRepository.save(existingProduct);
		// Act
		List<Product> products = productRepository.findAll();
		// Assert
		assertNotNull(products);
		assertEquals(5, products.size());

		Product updatedProduct = products.stream()
			.filter(p -> p.getId() != null && p.getId().equals(1L))
			.findFirst()
			.orElse(null);

		assertNotNull(updatedProduct);
		assertEquals("Updated Laptop", updatedProduct.getName());
	}

	@Test
	@Tag("valid")
	void findAllReturnsListWithAllProductNames() {
		// Arrange - repository is already initialized
		// Act
		List<Product> products = productRepository.findAll();
		// Assert
		assertNotNull(products);
		List<String> expectedNames = List.of("Laptop", "Mouse", "Keyboard", "Monitor", "Headphones");
		List<String> actualNames = products.stream().map(Product::getName).toList();

		assertTrue(actualNames.containsAll(expectedNames));
	}

	@Test
	@Tag("valid")
	void findAllReturnsProductsWithValidPrices() {
		// Arrange - repository is already initialized
		// Act
		List<Product> products = productRepository.findAll();
		// Assert
		assertNotNull(products);
		assertTrue(
				products.stream().allMatch(p -> p.getPrice() != null && p.getPrice().compareTo(BigDecimal.ZERO) > 0));
	}

	@Test
	@Tag("valid")
	void findAllReturnsProductsWithValidStock() {
		// Arrange - repository is already initialized
		// Act
		List<Product> products = productRepository.findAll();
		// Assert
		assertNotNull(products);
		assertTrue(products.stream().allMatch(p -> p.getStock() >= 0));
	}

}